# المواصفة التنفيذية الشاملة
# للنسخ الاحتياطي والاستعادة والمزامنة متعددة النسخ

## نظام إدارة مستودعات مديرية صحة دمشق

| الحقل | القيمة |
|---|---|
| نوع الوثيقة | مواصفة منتج ومعمارية وخطة تنفيذ واختبار وتشغيل |
| الإصدار | 1.0 |
| الحالة | جاهزة للتنفيذ على مراحل |
| نطاق التطبيق | Windows Desktop، Android APK Offline، Web Server |
| اللغة التشغيلية | العربية مع دعم RTL |
| مالك القرار | مسؤول النظام ومسؤول البيانات |
| قاعدة التغيير | لا ينفذ أي تغيير على الإنتاج قبل اجتياز بوابة المرحلة السابقة |

> **هذه الوثيقة هي المرجع التنفيذي الموحد.**  
> كل مرحلة أدناه تحتوي على: هدف، نطاق تنفيذ، اختبارات إلزامية، توثيق مطلوب، مخرجات، وبوابة اعتماد.  
> لا تعتبر المرحلة مكتملة بمجرد كتابة الكود؛ بل بعد نجاح الاختبارات وتسليم التوثيق واجتياز بوابة الاعتماد.

---

## 1. الملخص التنفيذي

المطلوب هو أن تكون كل نسخة من النظام مستقلة بقاعدة بيانات منفصلة، وتعمل دون الاعتماد على نسخة أخرى، ثم تستطيع مزامنة أي نسخة مع أي نسخة أخرى عند الحاجة:

```text
Windows Database  ↔  Android Database
       ↕                    ↕
Web PostgreSQL   ↔  Web PostgreSQL أخرى مستقبلاً
```

كل نسخة هي **عقدة مستقلة** `Node`، وليست مجرد واجهة لقاعدة مشتركة.

### المبدأ الحاكم

المزامنة لا تستبدل قاعدة بيانات بقاعدة أخرى، ولا تنقل آخر حالة فقط.  
المزامنة تتبادل التغييرات الموثقة منذ آخر نقطة معرفة، وتطبقها بطريقة:

- ثنائية الاتجاه.
- قابلة لإعادة التنفيذ دون تكرار.
- قابلة للاستكمال بعد انقطاع الاتصال.
- قابلة للتدقيق.
- واضحة في التعارضات.
- آمنة بالنسبة لأرصدة المخزون والعهد والدفعات.

### الفصل بين الوظائف

يجب تنفيذ نظامين متكاملين لكن منفصلين:

1. **النسخ الاحتياطي والاستعادة**
   - لحماية قاعدة كاملة أو إنشاء نقطة استعادة.
   - قد تحتوي على كل البيانات حتى لو لم تكن هناك عقدة أخرى.
   - تدعم الاستعادة الكاملة والدمج المحدود.

2. **المزامنة**
   - لتبادل التغييرات بين عقدتين مستقلتين.
   - لا تستبدل قاعدة البيانات.
   - تستخدم حزم تفاضلية صغيرة.
   - لا تعتبر ناجحة إلا بعد إتمام اتجاهي التبادل.

---

## 2. الوضع الحالي الذي يجب البناء عليه

هذه الخطة تراعي الوضع الحالي للمشروع ولا تفترض أن المنصات متطابقة:

| المكوّن | الوضع الحالي | أثره على التنفيذ |
|---|---|---|
| Web Server | Express + PostgreSQL + Drizzle | يحتاج Change Log وعقود مزامنة |
| النسخ المركزي | JSON إصدار 2.0، admin فقط | يحتاج صيغة موحدة وتشفير وواجهة استعادة |
| الاستعادة المركزية | Merge باستخدام `onConflictDoNothing` | لا تكفي للاستعادة الاحترافية ويجب استبدالها بمحرك تحقق |
| Web UI | تصدير دون استيراد كامل | يجب إضافة تدفق استعادة ومزامنة واضح |
| Windows | Electron + PGlite محلي | يحتاج هوية عقدة وسجل تغييرات وصيغة نقل |
| Android APK | Capacitor + IndexedDB Offline | يحتاج نموذجاً موحداً واستيراداً حقيقياً |
| Android Backup | تصدير حالة IndexedDB | غير كافٍ كنسخة موحدة وقد يحتوي بيانات حساسة |
| الربط الحالي | يمكن لبعض النسخ استخدام API خارجي | هذا اتصال بمصدر بيانات وليس مزامنة بين قاعدتين |

### قرار الترحيل

لا يتم حذف الصيغ الحالية مباشرة. يتم:

1. قراءتها في بيئة ترحيل معزولة.
2. فحصها.
3. تحويلها إلى الصيغة المعيارية الجديدة.
4. مطابقة الأرصدة والعلاقات.
5. اعتماد نقطة أساس `Baseline`.
6. إيقاف الصيغة القديمة بعد نجاح التشغيل التجريبي.

---

## 3. المتطلبات غير القابلة للتفاوض

### 3.1 متطلبات الاستقلالية

- لكل تثبيت قاعدة بيانات مستقلة.
- يمكن إنشاء المواد والحركات محلياً دون اتصال.
- لا تتوقف النسخة المحلية إذا تعطل الخادم.
- لا تتصل نسخة بقاعدة نسخة أخرى دون موافقة المستخدم أو سياسة تشغيل صريحة.
- يمكن أخذ نسخة كاملة من كل عقدة دون الحاجة إلى العقد الأخرى.

### 3.2 متطلبات المزامنة

- أي عقدتين قابلتان للمزامنة.
- المزامنة ثنائية الاتجاه.
- دعم الاتصال المباشر عند توفره.
- دعم Relay عبر خادم وسيط.
- دعم ملف مشفر عند عدم توفر الاتصال.
- لا تتكرر الحركة عند إرسال الحزمة مرتين.
- لا تضيع التغييرات عند انقطاع الاتصال.
- التغييرات المستلمة يمكن تمريرها لاحقاً إلى عقدة ثالثة.
- التعارضات الخطيرة تظهر للمراجعة ولا تحل بصمت.

### 3.3 متطلبات النسخ والاستعادة

- النسخ كاملة أو تفاضلية.
- الملف مضغوط ومشفر وموقع أو قابل للتحقق.
- لا يتضمن كلمات المرور أو الأسرار أو الجلسات.
- الاستعادة تبدأ بفحص ومعاينة.
- تؤخذ نسخة تلقائية قبل الاستعادة.
- توجد عملية `Dry Run`.
- توجد استعادة كاملة ودمج آمن.
- توجد نتيجة مفصلة لكل سجل: مطبق، مكرر، مرفوض، متعارض.
- يمكن التراجع عن الاستعادة من خلال نقطة ما قبل الاستعادة.

### 3.4 متطلبات المخزون

- دفتر الحركات هو المرجع الحسابي.
- الأرصدة لا تعدل مباشرة عند المزامنة دون حركة موثقة.
- FEFO والدفعات وتواريخ الصلاحية تبقى صحيحة.
- العهد الجزئية والكاملة تبقى صحيحة.
- التلف والمرتجع المركزي يبقيان مرتبطين بأصلهما.
- أي ناتج سالب أو غير منطقي يتحول إلى تعارض واضح.

---

## 4. المصطلحات

| المصطلح | التعريف |
|---|---|
| Node / عقدة | تثبيت مستقل للتطبيق بقاعدة بيانات خاصة |
| Node ID | معرف فريد للتثبيت وليس للمستخدم |
| Entity | كيان مثل مادة أو دفعة أو عهدة |
| Global ID | UUID ثابت للكيان عبر كل العقد |
| Operation ID | UUID للحركة أو الحدث التشغيلي |
| Change Log | سجل غير قابل للمحو للتغييرات |
| Outbox | تغييرات محلية لم ترسل بعد |
| Inbox | تغييرات وصلت ولم تطبق بعد أو طبقت |
| Tombstone | علامة حذف منطقية تمنع عودة سجل محذوف |
| Vector Clock | متجه يصف آخر تسلسل معروف من كل عقدة |
| Sync Session | جلسة مزامنة بين عقدتين |
| Sync Package | حزمة تغييرات تفاضلية |
| Full Backup | نسخة كاملة قابلة للاستعادة |
| Delta Backup | نسخة تحتوي على تغييرات منذ نقطة محددة |
| Baseline | نقطة أساس موحدة تبدأ منها المزامنة |
| Conflict | تغيير لا يمكن تطبيقه بأمان آلياً |
| Relay | خادم ينقل حزمة مشفرة بين عقدتين |

---

## 5. المعمارية المستهدفة

### 5.1 مكونات كل عقدة

كل عقدة يجب أن تحتوي منطقياً على:

```text
Application Data
Change Log
Outbox
Inbox
Sync Cursor
Conflict Queue
Backup Metadata
Node Identity
```

### 5.2 طبقات النظام

```text
واجهة المستخدم
    ↓
Sync / Backup Application Service
    ↓
Canonical Data Contract
    ↓
Local Database Adapter
    ↓
PostgreSQL أو PGlite أو IndexedDB
```

لا يجوز أن تتعامل واجهة المستخدم مباشرة مع شكل قاعدة PostgreSQL أو شكل حالة IndexedDB. يجب أن تمر كل المنصات عبر عقد بيانات موحد.

### 5.3 مصادر البيانات

لا يوجد مصدر وحيد دائم للحالة. توجد بدلاً منه:

- حالة محلية لكل عقدة.
- سجل تغييرات موزع.
- قواعد تعارض معلنة.
- جلسات مزامنة موثقة.

يمكن لمسؤول المؤسسة اعتماد عقدة معينة كمرجع تشغيلي في سياسة العمل، لكن ذلك قرار تشغيل وليس افتراضاً مخفياً داخل محرك المزامنة.

---

## 6. الهوية العالمية للسجلات

### 6.1 هوية العقدة

عند أول تشغيل لتثبيت جديد يتم إنشاء:

```text
nodeId
installationId
nodeType: windows | android | web
createdAt
keyId
```

ويجب:

- حفظ `nodeId` بشكل دائم.
- عدم تغييره عند تحديث التطبيق.
- عدم نسخه إلى جهاز آخر عند استنساخ قاعدة البيانات.
- توفير إجراء رسمي لتسجيل “نسخة مستنسخة” بمعرف جديد.
- تسجيل إعادة التثبيت أو إعادة التسجيل في سجل الإدارة.

### 6.2 هوية الكيانات

يجب أن يحصل كل كيان قابل للنقل على `globalId` مستقل عن المفتاح الرقمي المحلي:

- المادة.
- التجهيزة.
- التصنيف.
- الدفعة.
- الجهة.
- المستلم.
- سبب الإخراج.
- الحركة.
- تخصيص الدفعة.
- العهدة.
- استلام العهدة.
- التلف.
- المرتجع.
- الإعدادات القابلة للمزامنة.

يمكن الإبقاء على `localId` لأسباب توافقية، لكنه ليس مفتاح المزامنة.

### 6.3 هوية الحركات

كل حركة تحتاج إلى:

```text
operationId
globalId
originNodeId
originSequence
documentNumber
documentNumberScope
transactionType
createdAt
recordedBy
```

`operationId` لا يتغير ولا يعاد استخدامه حتى إذا فشلت المزامنة.

### 6.4 أرقام المستندات

رقم السند المرئي ليس معرفاً عالمياً، لأن عقدتين قد تعملان Offline.

يجب حفظ:

- `documentNumber`: الرقم المرئي.
- `documentNumberScope`: العقدة أو الفترة أو نوع الحركة.
- `operationId`: المعرف الفعلي.

عند تعارض رقم ظاهر، تبقى الحركتان ويظهر تعارض ترقيم، ولا تحذف أي حركة.

---

## 7. نموذج سجل التغييرات

### 7.1 الحقول الإلزامية

```text
changeId
operationId
entityType
entityGlobalId
changeType
payload
originNodeId
originSequence
causedByChangeId
parentRevision
createdAt
receivedAt
appliedAt
status
rejectionCode
```

### 7.2 أنواع التغييرات

```text
create
update
delete
correction
system-reconciliation
```

### 7.3 حالات التغيير

```text
local-pending
exported
received
validated
applied
duplicate
rejected
conflict
superseded
```

### 7.4 قواعد السجل

- سجل التغييرات Append-only.
- لا يتم تعديل Payload الأصلي.
- أي تصحيح ينشئ تغييراً جديداً.
- كل تغيير يملك مصدره وتسلسله.
- لا يطبق التغيير مرتين.
- الاحتفاظ بالتغييرات المستلمة ضروري لتمريرها إلى عقدة ثالثة.
- حذف التغيير من السجل ليس جزءاً من التشغيل العادي.

---

## 8. Vector Clock ونقاط المزامنة

كل عقدة تحتفظ بمتجه:

```json
{
  "node-a": 180,
  "node-b": 44,
  "node-c": 912
}
```

يمثل الرقم آخر `originSequence` معروف من كل عقدة.

### تحديد التغييرات الناقصة

عند مزامنة عقدتين:

1. تتبادل العقدتان المتجهين.
2. تحدد كل عقدة التغييرات التي تسلسلاتها أعلى من المتجه المقابل.
3. ترسل التغييرات الناقصة فقط.
4. يرسل كل طرف إشعار استلام وتطبيق.
5. يدمج الطرفان المتجه الجديد.

### حالات خاصة

- إذا كانت الحزمة أقدم من آخر مطبق، تفحص للتكرار ولا تعكس تغييرات أحدث.
- إذا كانت الحزمة تحتوي فراغاً في التسلسل، تؤجل التغييرات التابعة حتى يصل الجزء المفقود.
- إذا تعذر الحصول على جزء مفقود، تظهر جلسة غير مكتملة ولا تطبق حالة ناقصة على أنها مكتملة.

---

## 9. العقد المعياري للصيغ والملفات

### 9.1 الامتداد

الامتداد الرسمي:

```text
.dme-sync
```

وهو ملف مضغوط، مشفر، وموقع أو قابل للتحقق.

### 9.2 أنواع الحزم

```text
full-backup
delta-sync
restore-point
baseline-migration
sync-response
```

### 9.3 بنية الحزمة

```text
manifest.json
changes.jsonl
entities/
  categories.jsonl
  items.jsonl
  equipment.jsonl
  batches.jsonl
  transactions.jsonl
  allocations.jsonl
  custodies.jsonl
  custody-returns.jsonl
  damages.jsonl
  central-returns.jsonl
  recipients.jsonl
  settings.jsonl
conflicts.jsonl
checksums.json
signature.bin
```

### 9.4 محتوى Manifest

```json
{
  "formatVersion": "1.0",
  "schemaVersion": "sync-1",
  "packageType": "delta-sync",
  "packageId": "uuid",
  "sourceNodeId": "uuid",
  "targetNodeId": "uuid-or-null",
  "createdAt": "ISO-8601",
  "baseVector": {},
  "lastVector": {},
  "recordCounts": {},
  "encryption": {
    "algorithm": "AES-256-GCM",
    "kdf": "Argon2id"
  },
  "contentHash": "sha256"
}
```

### 9.5 التوافق

يجب رفض الحزمة بوضوح إذا:

- كان `formatVersion` غير مدعوم.
- كان `schemaVersion` غير قابل للترقية.
- كان التوقيع غير صحيح.
- كانت البصمة لا تطابق المحتوى.
- كانت الحزمة موجهة لعقدة أخرى دون صلاحية.
- كانت الحزمة منسوخة أو مطبقة سابقاً.
- كانت العلاقات أو أنواع الحقول غير صحيحة.

---

## 10. الأمان وإدارة الثقة

### 10.1 التشفير

يجب استخدام:

- AES-256-GCM لتشفير المحتوى.
- Argon2id أو PBKDF2 لاشتقاق مفتاح من كلمة المرور.
- Salt عشوائي لكل ملف.
- Nonce فريد لكل عملية تشفير.
- عدم حفظ كلمة المرور داخل الملف.

### 10.2 التوقيع والبصمة

كل حزمة يجب أن تحتوي على:

- SHA-256 للمحتوى.
- توقيع للحزمة أو HMAC في البيئات التي تستخدم مفتاحاً مشتركاً.
- معرف المفتاح المستخدم.
- تاريخ إصدار أو انتهاء صلاحية عند الحاجة.

### 10.3 الاقتران بين عقدتين

يجب أن يدعم النظام:

- QR أو رمزاً قصيراً مؤقتاً.
- صلاحية محدودة للرمز.
- إظهار اسم ونوع العقدة الأخرى.
- موافقة صريحة من الطرفين.
- إلغاء الثقة.
- قائمة بالعقد الموثوقة.
- سجل آخر جلسات المزامنة.

### 10.4 الصلاحيات

الصلاحيات المقترحة:

| الصلاحية | الوظيفة |
|---|---|
| `backup.create` | إنشاء نسخة |
| `backup.inspect` | فحص نسخة |
| `backup.restore` | استعادة |
| `sync.start` | بدء جلسة |
| `sync.apply` | تطبيق حزمة |
| `sync.resolve` | حل تعارض |
| `sync.manage-trusted-nodes` | إدارة العقد الموثوقة |
| `sync.audit` | عرض سجل المزامنة |

الاستعادة الكاملة وحل تعارضات المخزون تتطلبان دوراً إدارياً أو صلاحية خاصة.

---

## 11. طرق المزامنة وتجربة المستخدم

### 11.1 المزامنة المباشرة

1. فتح **الإعدادات ← المزامنة**.
2. اختيار **إضافة عقدة** أو اختيار عقدة موثوقة.
3. إنشاء جلسة.
4. إظهار رمز أو QR.
5. موافقة الطرف الآخر.
6. تبادل المتجهات.
7. تحليل الاختلافات.
8. عرض Preview في الاتجاهين.
9. اعتماد الطرفين.
10. تطبيق ذري.
11. إنشاء تقرير.

### 11.2 المزامنة عبر Relay

يستخدم الخادم كوسيط نقل:

1. العقدة A تنشئ حزمة موجهة إلى B.
2. ترفعها مشفرة.
3. العقدة B تنزلها.
4. تفكها محلياً وتعاينها.
5. تطبقها أو ترفضها.
6. تنشئ B حزمة رد.
7. تعود حزمة الرد إلى A.
8. تعتبر الجلسة مكتملة بعد تأكيد الاتجاهين.

يفضل أن يكون محتوى الحزمة مشفراً من طرف إلى طرف حتى لا يستطيع Relay قراءة بيانات المخزون.

### 11.3 المزامنة اليدوية

في حال USB أو نقل ملف:

```text
A: إنشاء حزمة
→ نقل الملف
B: فحص ومعاينة وتطبيق
→ إنشاء حزمة رد
→ نقل الرد
A: فحص ومعاينة وتطبيق
→ إغلاق الجلسة
```

يجب حفظ معرف الجلسة حتى لا يخلط المستخدم بين حزم جلسات مختلفة.

### 11.4 حالات الجلسة

```text
created
pairing
waiting-for-peer
prepared
preview-ready
approved
applying
partially-applied
waiting-for-response
completed
rejected
expired
failed
```

---

## 12. خوارزمية تطبيق الحزمة

يجب أن ينفذ محرك التطبيق الخطوات التالية بالترتيب:

### الخطوة 1: الفحص البنيوي

- الامتداد.
- الحجم.
- Manifest.
- الإصدار.
- البصمة.
- التوقيع.
- كلمة المرور.

### الخطوة 2: الفحص المنطقي

- الحقول الإلزامية.
- أنواع القيم.
- UUID.
- المراجع الخارجية.
- التسلسل الزمني.
- عدم وجود Payload غير مسموح.

### الخطوة 3: التصنيف

تصنف كل عملية إلى:

```text
safe-to-apply
duplicate
needs-merge
conflict
invalid
```

### الخطوة 4: المعاينة

تعرض الواجهة:

- عدد الإضافات.
- عدد التحديثات.
- عدد الحذف المنطقي.
- عدد المكرر.
- عدد التعارضات.
- أثر المخزون.
- أثر العهد.
- العمليات التي ستؤجل.

### الخطوة 5: الاعتماد

لا يتم التطبيق قبل موافقة المستخدم المخول.

### الخطوة 6: التطبيق الذري

- أخذ نقطة استعادة قبلية.
- تطبيق التغييرات المقبولة داخل Transaction.
- تسجيل التغييرات في Inbox وChange Log.
- تحديث الأرصدة المشتقة.
- تحديث Vector Clock.
- إنشاء تقرير.

### الخطوة 7: ما بعد التطبيق

- فحص توازن دفتر المخزون.
- فحص FEFO.
- فحص العهد.
- فحص المراجع المعلقة.
- إبقاء التعارضات في Conflict Queue.
- عدم إعلان الجلسة مكتملة إذا بقيت أجزاء غير مطبقة دون توضيح.

---

## 13. سياسة التعارضات

### 13.1 الحركات التشغيلية

الحركات التالية Append-only:

- إدخال.
- إخراج.
- استلام.
- تسليم عهدة.
- استلام عهدة.
- تلف.
- مرتجع مركزي.
- تسوية.
- تصحيح.

إذا وصل نفس `operationId` مرة أخرى، تكون النتيجة `duplicate` وليس حركة جديدة.

### 13.2 التعارضات الخطيرة

يجب إدخال العملية إلى Conflict Queue إذا:

- سببت رصيداً سالباً.
- أشارت إلى دفعة غير موجودة.
- أشارت إلى مادة محذوفة منطقياً.
- تجاوزت استلام العهدة الكمية المسلمة.
- خالفت قواعد FEFO غير القابلة للتجاوز.
- امتلكت نفس رقم المستند ضمن نفس النطاق مع مصدر مختلف.
- كان هناك تعديل متزامن على نفس الحقل الحساس.

### 13.3 البيانات التعريفية

| نوع التغيير | القاعدة |
|---|---|
| إنشاء كيانين مستقلين | يحتفظ بهما النظام مع تنبيه تشابه |
| تعديل حقول مختلفة | دمج تلقائي |
| تعديل نفس الحقل | تعارض للمراجعة |
| حذف كيان مستخدم | Tombstone فقط |
| تعديل الاسم أو الوصف | يمكن اعتماد سياسة آخر تعديل مع سجل مصدر |
| تعديل كود الصنف | يتطلب صلاحية ومراجعة |

### 13.4 حل التعارض

يجب أن يختار المستخدم أحد الخيارات:

- اعتماد نسخة A.
- اعتماد نسخة B.
- دمج يدوي.
- إنشاء تصحيح جديد.
- إبقاء التعارض مؤجلاً.

كل قرار يحتاج:

- المستخدم.
- التاريخ.
- السبب.
- السجلات المتأثرة.
- الحزمة والجلسة.

---

## 14. النسخ الاحتياطي والاستعادة

## 14.1 أنواع النسخ

### Full Backup

يحتوي على:

- البيانات التشغيلية.
- Change Log.
- Inbox وOutbox الضروريان.
- Vector Clock.
- إعدادات المزامنة.
- سجل التدقيق المسموح.
- Manifest.

لا يحتوي على:

- كلمات المرور.
- Hashes أو salts الخاصة بالمستخدمين.
- Session cookies.
- مفاتيح تشفير الجهاز.
- أسرار بيئة التشغيل.

### Delta Backup

يحتوي على التغييرات منذ:

- Vector Clock محدد.
- أو نقطة زمنية.
- أو `changeId` محدد.

### Restore Point

نسخة تلقائية قبل:

- استعادة.
- تطبيق حزمة كبيرة.
- ترقية مخطط.
- ترحيل صيغة.

### 14.2 سياسة الاحتفاظ

يجب دعم سياسة قابلة للضبط، ويفضل افتراضياً:

- 30 نسخة يومية.
- 12 نسخة أسبوعية.
- 12 نسخة شهرية.
- عدم حذف آخر نسخة سليمة.
- عدم حذف نسخة قبل نجاح النسخة التالية والتحقق من بصمتها.

### 14.3 الاستعادة الكاملة

تستخدم في:

- جهاز جديد.
- قاعدة فارغة.
- كارثة كاملة.
- إنشاء عقدة من نقطة أساس.

قواعدها:

- لا تستبدل قاعدة مستخدمة دون تأكيد متعدد الخطوات.
- يجب حفظ نسخة الوضع الحالي.
- يجب فحص توافق المخطط.
- يجب تشغيل فحص شامل بعد التطبيق.

### 14.4 الاستعادة بالدمج

تستخدم لإدخال تغييرات أو كيانات من نسخة أخرى، ولا تعني أن النسخة القديمة هي الحالة الصحيحة بالكامل.

يجب أن تعتمد على:

- `globalId`.
- `operationId`.
- Revision.
- Conflict Queue.

ولا تعتمد على تجاهل التعارضات بصمت.

### 14.5 المعاينة وDry Run

قبل أي Restore يجب إظهار:

```text
السجلات الجديدة
السجلات المعدلة
السجلات المتكررة
السجلات المرفوضة
التعارضات
تغير أرصدة المواد
تغير العهد
تغير عدد الدفعات
```

`Dry Run` يفحص ويحسب النتيجة دون تغيير قاعدة البيانات.

---

## 15. قواعد خاصة بكل منصة

### 15.1 Windows

يجب:

- توليد `nodeId` عند أول تشغيل.
- حفظ قاعدة PGlite في مجلد التطبيق المخصص.
- إيقاف الاتصالات بأمان قبل النسخة الفيزيائية.
- توفير نسخة منطقية معيارية للنقل.
- توفير Import وExport بصيغة `.dme-sync`.
- دعم المزامنة المباشرة واليدوية.
- منع استخدام نسخ مجلد PGlite كبديل للمزامنة.

### 15.2 Android APK Offline

يجب:

- استبدال تصدير حالة IndexedDB الخام بصيغة معيارية.
- إضافة Import ظاهر في الواجهة.
- منع تضمين كلمات المرور وبيانات الجلسة.
- تخزين Change Log وOutbox في IndexedDB.
- دعم الدفعات والتخصيصات والعهد والتلف والمرتجع بنفس العقد المنطقي.
- الاحتفاظ بجلسة مزامنة غير مكتملة بعد إغلاق التطبيق.
- تحذير المستخدم قبل مسح بيانات التطبيق.

### 15.3 Web Server

يجب:

- تخزين Change Log في PostgreSQL.
- توفير API لجلسات المزامنة.
- استخدام رفع ملفات Streaming للحزم الكبيرة.
- توفير Relay للحزم المشفرة.
- توفير نسخ مجدولة.
- تسجيل كل عمليات النسخ والمزامنة والاستعادة.
- فصل صلاحيات إنشاء النسخة عن تطبيقها.

---

## 16. عقود API المقترحة

هذه أسماء وظيفية مقترحة، ويجب تثبيتها في OpenAPI قبل التنفيذ:

### العقدة والثقة

```text
GET  /api/sync/node
POST /api/sync/nodes/pair
GET  /api/sync/nodes
POST /api/sync/nodes/:nodeId/revoke
```

### الجلسات

```text
POST /api/sync/sessions
GET  /api/sync/sessions/:sessionId
POST /api/sync/sessions/:sessionId/approve
POST /api/sync/sessions/:sessionId/complete
POST /api/sync/sessions/:sessionId/cancel
```

### تبادل التغييرات

```text
POST /api/sync/sessions/:sessionId/handshake
POST /api/sync/sessions/:sessionId/manifest
POST /api/sync/sessions/:sessionId/packages
GET  /api/sync/sessions/:sessionId/packages/:packageId
POST /api/sync/sessions/:sessionId/ack
```

### التعارضات

```text
GET  /api/sync/conflicts
GET  /api/sync/conflicts/:conflictId
POST /api/sync/conflicts/:conflictId/resolve
```

### النسخ الاحتياطية

```text
GET  /api/backups
POST /api/backups
POST /api/backups/inspect
POST /api/backups/dry-run
POST /api/backups/restore
GET  /api/backups/:backupId/report
POST /api/backups/:backupId/rollback
```

يجب أن تكون كل العقود:

- موصوفة في OpenAPI.
- محمية بالصلاحيات.
- قابلة للتدقيق.
- قابلة للتعامل مع انقطاع الاتصال.
- مدعومة برسائل خطأ ثابتة.

---

## 17. مراحل التنفيذ والاختبار والتوثيق

## المرحلة 0 — اعتماد المتطلبات وخط الأساس

### الهدف

تحويل هذه الوثيقة إلى خط أساس رسمي قبل تعديل قاعدة البيانات أو واجهات المنصات.

### أعمال التنفيذ

- اعتماد مفهوم العقد المستقلة.
- اعتماد أن المزامنة ثنائية الاتجاه.
- اعتماد أن الحركة Append-only.
- اعتماد أن النسخ والاستعادة منفصلان عن المزامنة.
- حصر كل الجداول الحالية.
- حصر كل العمليات الحالية.
- حصر الاختلاف بين PostgreSQL وPGlite وIndexedDB.
- تحديد الحقول الحساسة.
- تحديد صلاحيات الإدارة.

### الاختبارات

- مراجعة متطلبات مكتوبة من مسؤول البيانات.
- اختبار أن كل حالة استخدام رئيسية مذكورة.
- اختبار عدم وجود تعارض بين سياسة FEFO وسياسة المزامنة.
- مراجعة قائمة الجداول والعلاقات.
- توقيع محضر اعتماد المتطلبات.

### التوثيق المطلوب

- تحديث هذه الوثيقة بالقرارات المعتمدة.
- إنشاء سجل قرارات `Decision Log`.
- إنشاء جدول توافق المنصات.
- إنشاء قاموس بيانات أولي.

### المخرجات

- متطلبات معتمدة.
- قائمة جداول.
- قائمة عمليات.
- سياسة تعارض أولية.

### بوابة النجاح

لا تبدأ المرحلة 1 قبل اعتماد مسؤول النظام ومسؤول البيانات للمتطلبات وقائمة المخاطر.

---

## المرحلة 1 — جرد النموذج الحالي وتثبيت قواعد المخزون

### الهدف

منع بناء المزامنة فوق اختلافات غير موثقة بين النسخ الحالية.

### أعمال التنفيذ

- استخراج مخطط PostgreSQL الحالي.
- مراجعة مخطط PGlite.
- توثيق حالة IndexedDB.
- مقارنة الحقول والأنواع والعلاقات.
- تحديد الجداول الناقصة في Android.
- تحديد القيم التي يتم حسابها ولا تخزن.
- تثبيت معادلات:
  - رصيد المادة.
  - رصيد التجهيزة.
  - رصيد الدفعة.
  - رصيد العهدة.
  - التلف.
  - المرتجع.
- تحديد البيانات التي لا يجوز نقلها.

### الاختبارات

- اختبار مطابقة الأرصدة مع دفتر الحركات.
- اختبار FEFO على دفعة واحدة وعدة دفعات.
- اختبار الدفعة منتهية الصلاحية.
- اختبار الاستلام الجزئي والكامل للعهد.
- اختبار التلف والمرتجع.
- اختبار المعدات ذات الرقم التسلسلي.
- مقارنة تقرير الخادم مع التقرير المحلي للبيانات نفسها.

### التوثيق المطلوب

- `Data Dictionary`.
- `Platform Parity Matrix`.
- `Inventory Invariants`.
- مخطط علاقات موحد.
- جدول الحقول الحساسة.

### المخرجات

- نموذج معياري معتمد.
- قائمة فجوات يجب إغلاقها في APK وWindows.
- مجموعة اختبارات مرجعية.

### بوابة النجاح

لا تبدأ الهوية العالمية قبل أن تكون قواعد الأرصدة قابلة للاختبار آلياً على المنصات الثلاث.

---

## المرحلة 2 — هوية العقد والسجلات العالمية

### الهدف

إزالة الاعتماد على الأرقام المحلية كمفاتيح للمزامنة.

### أعمال التنفيذ

- إضافة هوية تثبيت مستقرة لكل منصة.
- إضافة `globalId`.
- إضافة `operationId`.
- إضافة `originNodeId`.
- إضافة `originSequence`.
- تحديد طريقة إنشاء UUID الآمنة.
- إضافة ربط بين `localId` و`globalId`.
- معالجة السجلات القديمة التي لا تملك UUID.
- إنشاء سياسة للنسخ المستنسخة.

### الاختبارات

- إنشاء نفس اسم مادة في عقدتين والتأكد من اختلاف الهوية.
- نقل نفس المادة والتأكد من ثبات `globalId`.
- إنشاء حركتين Offline والتأكد من اختلاف `operationId`.
- إعادة فتح التطبيق والتأكد من ثبات `nodeId`.
- نسخ قاعدة اختبار والتأكد من عدم عمل العقدتين بهوية واحدة.
- اختبار الترحيل على نسخة من بيانات الإنتاج.

### التوثيق المطلوب

- مواصفة `Node Identity`.
- مواصفة `Global Identity`.
- مواصفة `Operation Identity`.
- دليل إعادة تسجيل جهاز مستنسخ.
- سجل تغيير مخطط قاعدة البيانات.

### المخرجات

- Migration آمن.
- اختبارات UUID.
- تعريف موحد للهوية.

### بوابة النجاح

لا يوجد أي تصادم في المعرفات عند إنشاء بيانات مستقلة في ثلاث عقد اختبار.

---

## المرحلة 3 — Change Log وOutbox وInbox

### الهدف

امتلاك مصدر موثوق لمعرفة ما تغير وما تم إرساله وما تم تطبيقه.

### أعمال التنفيذ

- إنشاء جدول Change Log في PostgreSQL وPGlite.
- إنشاء مخزن مكافئ في IndexedDB.
- إنشاء Outbox.
- إنشاء Inbox.
- إنشاء Sync Cursors.
- إنشاء Conflict Queue.
- إضافة Tombstones.
- تسجيل تغييرات الحركة والبيانات التعريفية.
- ضمان أن التغيير وسجل العمل يكتبان ذرياً.

### الاختبارات

- إنشاء حركة محلية والتأكد من وجودها في Outbox.
- إغلاق التطبيق قبل الإرسال والتأكد من بقاء التغيير.
- استلام تغيير والتأكد من تسجيله في Inbox.
- إعادة استلام التغيير والتأكد من عدم تكراره.
- حذف منطقي ثم مزامنة ثم التأكد من عدم عودة السجل.
- إدخال تغيير من A إلى B ثم B إلى C والتأكد من انتقاله.
- فشل كتابة السجل واختبار التراجع الذري.

### التوثيق المطلوب

- مخطط جداول Change Log.
- دورة حياة التغيير.
- دورة حياة الجلسة.
- كتالوج رموز الرفض والتعارض.
- سياسة الاحتفاظ بالسجل.

### المخرجات

- طبقة سجل تغييرات مشتركة منطقياً.
- اختبارات Outbox/Inbox.
- أدوات فحص السجل.

### بوابة النجاح

يمكن استعادة واستكمال Outbox بعد إغلاق مفاجئ دون فقد أو تكرار.

---

## المرحلة 4 — الصيغة المعيارية والتشفير

### الهدف

توفير ملف واحد مفهوم وآمن لكل المنصات.

### أعمال التنفيذ

- تثبيت `.dme-sync`.
- تطبيق Manifest.
- تطبيق `changes.jsonl`.
- تطبيق Checksums.
- تطبيق التوقيع أو HMAC.
- تطبيق الضغط.
- تطبيق AES-256-GCM.
- تطبيق KDF آمن.
- منع الأسرار من الحزمة.
- دعم `full-backup` و`delta-sync` و`baseline-migration`.
- إنشاء Reader/Writer مستقل عن المنصة.

### الاختبارات

- تصدير واستيراد حزمة بين نفس المنصة.
- تصدير Windows واستيراده في Android.
- تصدير Android واستيراده في Web.
- تغيير بايت داخل الملف والتأكد من الرفض.
- كلمة مرور خاطئة والتأكد من الرفض.
- حزمة غير مكتملة والتأكد من الرفض.
- حزمة إصدار قديم ضمن سياسة التوافق.
- حزمة كبيرة واختبار الذاكرة والوقت.
- التأكد من عدم وجود كلمات مرور أو أسرار في محتوى الحزمة.

### التوثيق المطلوب

- مواصفة تنسيق الحزمة.
- مواصفة التشفير.
- مواصفة التوقيع والبصمة.
- جدول توافق الإصدارات.
- دليل إنشاء وفحص حزمة.

### المخرجات

- مكتبة تنسيق مشتركة.
- أمثلة حزم اختبارية غير حساسة.
- أدوات فحص الحزمة.

### بوابة النجاح

تقرأ المنصات الثلاث الحزمة نفسها وتتحقق من سلامتها دون اختلاف في النتيجة.

---

## المرحلة 5 — محرك الاستعادة

### الهدف

تحويل الاستعادة من إدخال صامت إلى عملية آمنة قابلة للفحص والتراجع.

### أعمال التنفيذ

- إضافة فحص بنيوي ومنطقي.
- إضافة `Dry Run`.
- إضافة Preview.
- إضافة Full Restore.
- إضافة Safe Merge.
- إضافة نسخة قبلية تلقائية.
- تسجيل تقرير لكل سجل.
- تطبيق المعاملة الذرية.
- تحديث التسلسلات والمراجع.
- تشغيل فحوص الأرصدة بعد الاستعادة.
- توفير Rollback من Restore Point.

### الاختبارات

- استعادة إلى قاعدة فارغة.
- دمج مع قاعدة تحتوي سجلات أحدث.
- وجود سجل مكرر.
- وجود سجل معدل في الطرفين.
- مرجع إلى دفعة مفقودة.
- عملية تنتج رصيداً سالباً.
- فشل منتصف الاستعادة.
- انقطاع التطبيق أثناء الاستعادة.
- فشل التوقيع أو التشفير.
- التراجع إلى النسخة القبلية.
- التحقق من أرصدة المواد والعهد بعد الاستعادة.

### التوثيق المطلوب

- دليل مسؤول الاستعادة.
- دليل اختيار Full Restore أو Merge.
- قاموس نتائج الاستعادة.
- Runbook للتراجع.
- سجل كل استعادة.

### المخرجات

- API استعادة جديد.
- واجهة استعادة.
- تقرير قابل للتنزيل.
- اختبارات Recovery.

### بوابة النجاح

لا يمكن أن تؤدي استعادة فاشلة إلى تغيير جزئي غير معلن، ولا يمكن اعتماد استعادة دون Preview.

---

## المرحلة 6 — النسخ التلقائي والنسخ التفاضلي

### الهدف

حماية كل عقدة حتى عندما لا توجد مزامنة مستمرة.

### أعمال التنفيذ

- إنشاء Full Backup محلي.
- إنشاء Delta Backup.
- جدولة النسخ في Web Server.
- تشغيل نسخة قبل Restore وMigration وSync كبير.
- تطبيق سياسة الاحتفاظ.
- حفظ Metadata النسخ.
- توفير فحص دوري للبصمات.
- دعم تصدير النسخة إلى مسار يحدده المستخدم في Windows وAndroid.

### الاختبارات

- إنشاء نسخة أثناء وجود تغييرات معلقة.
- استعادة Full ثم تطبيق Delta.
- حذف ملف واحد والتأكد من اكتشافه.
- تلف النسخة الأخيرة والتأكد من عدم حذف النسخة السابقة السليمة.
- امتلاء المساحة.
- إغلاق الجهاز أثناء النسخ.
- الاحتفاظ بعدد النسخ المحدد.
- فحص تطابق العدد والبصمة.

### التوثيق المطلوب

- سياسة الاحتفاظ.
- سياسة 3-2-1.
- دليل النسخ المجدول.
- دليل استعادة نقطة زمنية.
- نموذج سجل فحص النسخ.

### المخرجات

- Backup Scheduler.
- Backup Catalog.
- تقرير صحة النسخ.

### بوابة النجاح

يمكن استعادة عقدة كاملة من آخر Full Backup ثم سلسلة Delta دون فقدان تغييرات.

---

## المرحلة 7 — المزامنة الثنائية الأساسية

### الهدف

تبادل التغييرات بين عقدتين مستقلتين دون الحاجة إلى قاعدة مشتركة.

### أعمال التنفيذ

- تطبيق Handshake.
- تبادل Vector Clock.
- تحديد الفروقات.
- إنشاء حزمة Delta.
- تطبيق Idempotency.
- تحديث Inbox وOutbox.
- تحديث Cursor.
- إرسال ACK.
- دعم الاستكمال.
- إنشاء Sync Report.

### الاختبارات

- A إلى B في اتجاه واحد.
- B إلى A في الاتجاه الآخر.
- تغييرات مستقلة في الطرفين.
- إرسال الحزمة مرتين.
- إرسال الحزم بترتيب مختلف.
- وجود Gap في التسلسل.
- انقطاع أثناء الرفع.
- انقطاع أثناء التطبيق.
- إعادة تشغيل جلسة غير مكتملة.
- مزامنة A ثم B ثم C.
- مزامنة ثلاث عقد بترتيبات مختلفة والتأكد من النتيجة.

### التوثيق المطلوب

- بروتوكول Handshake.
- بروتوكول Delta Exchange.
- حالات جلسة المزامنة.
- دليل استكمال الجلسة.
- رموز نتائج المزامنة.

### المخرجات

- Sync Engine.
- Sync Session Store.
- تقرير مزامنة.

### بوابة النجاح

تعمل المزامنة الثنائية في بيئة اختبار دون تكرار أو فقدان، وتبقى القاعدة صالحة عند الانقطاع.

---

## المرحلة 8 — المزامنة اليدوية وRelay

### الهدف

تمكين المستخدم من المزامنة حتى عندما لا يمكن للطرفين الاتصال مباشرة.

### أعمال التنفيذ

- إنشاء Pairing Code أو QR.
- إنشاء حزمة موجهة.
- رفع حزمة Relay مشفرة.
- تنزيل الحزمة.
- إنشاء Sync Response.
- ربط الرد بالجلسة الأصلية.
- دعم نقل الملف يدوياً.
- انتهاء صلاحية الجلسة.
- منع خلط حزم جلسات مختلفة.

### الاختبارات

- Windows يرسل إلى Android عبر ملف.
- Android يرد إلى Windows.
- Android يرسل إلى Web عبر Relay.
- Web يرد إلى Android.
- ملف موجه لعقدة خاطئة.
- ملف من جلسة منتهية.
- ملف مكرر.
- ملف تم تعديله.
- فقدان الرد ثم إعادة إنشائه.
- إكمال جلسة بعد عدة أيام.

### التوثيق المطلوب

- دليل المزامنة بUSB.
- دليل Relay.
- دليل الاقتران وإلغاء الثقة.
- دليل استكشاف الحزم العالقة.
- سياسة حذف الحزم من Relay.

### المخرجات

- File Sync UI.
- Relay API.
- إدارة جلسات يدوية.

### بوابة النجاح

يمكن إكمال جلسة ثنائية الاتجاه بدون اتصال مباشر، مع تقرير واضح لحالة كل اتجاه.

---

## المرحلة 9 — قواعد التعارضات والتسويات

### الهدف

حماية المخزون من نتائج المزامنة غير المنطقية.

### أعمال التنفيذ

- إنشاء Conflict Queue.
- تصنيف التعارض حسب الخطورة.
- تطبيق قواعد الحركات Append-only.
- تطبيق قواعد البيانات التعريفية.
- منع الرصيد السالب التلقائي.
- إضافة شاشة مراجعة التعارض.
- إضافة قرار اعتماد أو رفض أو تصحيح.
- ربط القرار بالمستخدم والجلسة.

### الاختبارات

- نفس العملية مرتين.
- إخراجان Offline من نفس الرصيد.
- استلام عهدة أكبر من المسلّم.
- تعديل نفس مادة في عقدتين.
- حذف مادة مرتبطة بحركة.
- اختلاف الدفعات.
- تعارض رقم سند.
- قرار المستخدم واعتماد التصحيح.
- إعادة مزامنة التعارض المحلول.

### التوثيق المطلوب

- مصفوفة قرارات التعارض.
- دليل مسؤول حل التعارض.
- نماذج رسائل المستخدم.
- تقرير أثر التعارض على المخزون.

### المخرجات

- Conflict Resolver.
- شاشة التعارضات.
- سجل قرارات.

### بوابة النجاح

لا يمكن لتعارض مخزني خطير أن يمر بصمت أو يخفض الرصيد دون سجل وقرار.

---

## المرحلة 10 — تكامل Windows

### الهدف

إدخال نسخة Windows المحلية في النظام الموحد.

### أعمال التنفيذ

- إضافة هوية العقدة.
- إضافة Change Log إلى PGlite.
- دعم الصيغة الموحدة.
- دعم Full وDelta Backup.
- إضافة Import/Export.
- ربط واجهة المزامنة.
- ضمان إغلاق قاعدة PGlite بأمان.
- دعم استعادة جهاز جديد.

### الاختبارات

- تشغيل Windows Offline عدة أيام.
- مزامنة Windows مع Windows.
- مزامنة Windows مع Android.
- مزامنة Windows مع Web.
- استعادة نسخة كاملة.
- استعادة Delta.
- انقطاع الكهرباء.
- قفل ملف قاعدة البيانات.
- تحديث التطبيق مع الاحتفاظ بالهوية والبيانات.

### التوثيق المطلوب

- دليل تثبيت Windows كعقدة جديدة.
- دليل إنشاء نسخة Windows.
- دليل نقل الحزمة عبر USB.
- دليل إعادة تسجيل جهاز مستنسخ.
- دليل الطوارئ.

### المخرجات

- إصدار Windows متوافق.
- Migration محلي.
- اختبارات سطح المكتب.

### بوابة النجاح

تعمل نسخة Windows مستقلة وتدخل جلسة مزامنة مكتملة مع كل منصة مستهدفة.

---

## المرحلة 11 — تكامل Android APK Offline

### الهدف

إدخال APK في نموذج البيانات والمزامنة الموحدين.

### أعمال التنفيذ

- إضافة Node Identity في IndexedDB.
- إضافة Change Log وOutbox وInbox.
- إضافة الكيانات الناقصة للدفعات والعهد والتخصيصات.
- استبدال التصدير الخام بصيغة `.dme-sync`.
- إضافة Import UI.
- حفظ الجلسة عند إغلاق التطبيق.
- دعم الملفات الكبيرة عبر تدفق مناسب.
- حماية النسخة المحلية.

### الاختبارات

- إنشاء حركات دون إنترنت.
- إغلاق التطبيق وفتح الجلسة.
- تصدير واستيراد في نفس الجهاز.
- Android إلى Windows.
- Android إلى Web.
- ملف خاطئ أو متضرر.
- مساحة تخزين قليلة.
- دورة مزامنة كاملة مع رد.
- مطابقة FEFO والعهد والتلف والمرتجع.

### التوثيق المطلوب

- دليل المستخدم على Android.
- دليل استيراد وتصدير الحزم.
- دليل نقل البيانات عند تغيير الهاتف.
- دليل مسح بيانات التطبيق بأمان.
- مصفوفة توافق إصدار APK.

### المخرجات

- APK متوافق.
- محول من IndexedDB القديم.
- اختبارات Android.

### بوابة النجاح

لا يعتمد الإصدار الجديد قبل نجاح دورة كاملة Android ↔ Windows وAndroid ↔ Web.

---

## المرحلة 12 — تكامل Web Server وواجهة الإدارة

### الهدف

توفير إدارة مركزية للمزامنة دون تحويل الخادم إلى مصدر وحيد مفروض.

### أعمال التنفيذ

- إضافة API حسب العقود المعتمدة.
- إضافة واجهة النسخ الاحتياطي.
- إضافة واجهة الاستعادة.
- إضافة واجهة المزامنة.
- إضافة واجهة العقد الموثوقة.
- إضافة واجهة التعارضات.
- إضافة Relay.
- إضافة النسخ المجدول.
- إضافة تقارير التدقيق.
- رفع حدود الملفات بشكل آمن.

### الاختبارات

- صلاحيات كل دور.
- جلسة مباشرة.
- جلسة Relay.
- استعادة من ملف.
- Dry Run دون تغيير.
- فشل الجلسة مع إعادة المحاولة.
- تنزيل تقرير.
- تطبيق حزمة كبيرة.
- اختبار CORS والجلسات.
- اختبار النشر على Replit.

### التوثيق المطلوب

- دليل مسؤول الخادم.
- OpenAPI محدث.
- دليل الصلاحيات.
- دليل Relay.
- Runbook للنسخ والاستعادة.
- Runbook للحوادث.

### المخرجات

- API موثق.
- واجهة إدارة كاملة.
- سجل تدقيق وتشغيل.

### بوابة النجاح

يمكن لمسؤول مخول إدارة المزامنة والنسخ والاستعادة دون استدعاءات API يدوية.

---

## المرحلة 13 — الترحيل من الصيغ الحالية

### الهدف

نقل البيانات الحالية دون فقدان أو خلط بين الصيغ.

### أعمال التنفيذ

- إنشاء بيئة ترحيل معزولة.
- قراءة Backup الخادم الحالي 2.0.
- قراءة تصدير APK الحالي.
- قراءة بيانات PGlite المحلية.
- إنشاء `Baseline` لكل عقدة.
- إنشاء UUID للكيانات القديمة.
- ربط السجلات المتشابهة وفق قواعد مراجعة.
- الاحتفاظ بخريطة `legacyId → globalId`.
- عدم دمج البيانات المتعارضة تلقائياً.
- إنشاء تقرير ترحيل.

### الاختبارات

- ترحيل نسخة اختبار صغيرة.
- ترحيل بيانات الحركات الوهمية.
- مطابقة إجمالي الحركات.
- مطابقة الأرصدة النهائية.
- مطابقة عدد الدفعات.
- مطابقة العهد المفتوحة.
- مطابقة الأجهزة ذات الرقم التسلسلي.
- اختبار سجلات ناقصة أو مكررة.
- اختبار استعادة Baseline.

### التوثيق المطلوب

- خطة الترحيل.
- قاموس تحويل الحقول.
- تقرير السجلات التي لم يمكن تحويلها.
- Runbook التراجع.
- محضر اعتماد Baseline.

### المخرجات

- أدوات Import Migration.
- Baseline معتمد.
- تقارير مطابقة.

### بوابة النجاح

لا يتم تشغيل المزامنة على بيانات حقيقية قبل اعتماد تقرير المطابقة وعدم وجود فرق غير مفسر.

---

## المرحلة 14 — اختبارات التحمل والأمن والتعافي

### الهدف

إثبات أن النظام يعمل في ظروف التشغيل الواقعية وليس فقط في الحالة المثالية.

### أعمال التنفيذ

- توليد بيانات كبيرة.
- اختبار حزم كبيرة.
- اختبار آلاف التغييرات المعلقة.
- اختبار أجهزة متعددة.
- اختبار انقطاع الشبكة.
- اختبار انقطاع الكهرباء.
- اختبار امتلاء المساحة.
- اختبار العبث بالملفات.
- اختبار الصلاحيات.
- اختبار سجلات التدقيق.

### الاختبارات

#### التحمل

- 10,000 تغيير في Outbox.
- 100,000 سجل في Full Backup.
- أكثر من جلسة متزامنة.
- تكرار الحزمة عشرات المرات.

#### التعافي

- إيقاف التطبيق أثناء التشفير.
- إيقاف التطبيق أثناء التحميل.
- إيقاف التطبيق أثناء Transaction.
- استعادة بعد تلف آخر نسخة.
- استكمال جلسة بعد فقدان ACK.

#### الأمن

- ملف معدل.
- كلمة مرور خاطئة.
- عقدة غير موثوقة.
- مستخدم بلا صلاحية.
- إعادة استخدام رمز Pairing.
- محاولة قراءة محتوى Relay.
- التأكد من عدم وجود أسرار في النسخ.

### التوثيق المطلوب

- تقرير اختبار التحمل.
- تقرير اختبار الأمن.
- تقرير اختبار التعافي.
- قائمة ثغرات وإجراءات علاج.
- حدود تشغيل موصى بها.

### المخرجات

- تقارير موثقة.
- حدود أداء.
- قائمة مخاطر متبقية.

### بوابة النجاح

لا توجد حالة فشل تؤدي إلى فقدان صامت أو فساد غير مكتشف للبيانات.

---

## المرحلة 15 — التشغيل التجريبي والإطلاق

### الهدف

إطلاق تدريجي مراقب قبل الاعتماد العام.

### أعمال التنفيذ

- اختيار عدد محدود من الأجهزة.
- تفعيل النسخ الاحتياطي قبل التشغيل.
- تعريف عقد تجريبية.
- تشغيل مزامنة يومية بملفات فعلية.
- تسجيل التعارضات.
- تنفيذ استعادة اختبارية دورية.
- تدريب المستخدمين.
- مراقبة الأداء والأخطاء.

### الاختبارات

- دورة تشغيل أسبوعية كاملة.
- مزامنة كل زوج من المنصات.
- استعادة عشوائية لنسخة.
- اختبار مستخدمين حقيقيين.
- اختبار فقدان جهاز.
- اختبار إضافة جهاز جديد.
- اختبار إلغاء جهاز.

### التوثيق المطلوب

- دليل المستخدم النهائي.
- دليل مسؤول النظام.
- سياسة المزامنة المؤسسية.
- سياسة النسخ والاحتفاظ.
- نموذج تسليم وتشغيل.
- تقرير Pilot.

### المخرجات

- تقرير Pilot.
- قائمة إصلاحات قبل الإطلاق.
- قرار Go / No-Go.

### بوابة النجاح

لا يتم التفعيل العام إلا بعد اعتماد مسؤول البيانات لتقرير التشغيل التجريبي.

---

## 18. مصفوفة الاختبار الشاملة

| المجال | Windows ↔ Android | Windows ↔ Web | Android ↔ Web | Full Restore | Delta Sync |
|---|---:|---:|---:|---:|---:|
| إنشاء مادة | نعم | نعم | نعم | نعم | نعم |
| إنشاء دفعة | نعم | نعم | نعم | نعم | نعم |
| إدخال | نعم | نعم | نعم | نعم | نعم |
| إخراج FEFO | نعم | نعم | نعم | نعم | نعم |
| تسليم عهدة | نعم | نعم | نعم | نعم | نعم |
| استلام جزئي | نعم | نعم | نعم | نعم | نعم |
| استلام كامل | نعم | نعم | نعم | نعم | نعم |
| تلف | نعم | نعم | نعم | نعم | نعم |
| مرتجع مركزي | نعم | نعم | نعم | نعم | نعم |
| جهاز Serial | نعم | نعم | نعم | نعم | نعم |
| تعارض رصيد | نعم | نعم | نعم | نعم | نعم |
| تكرار حزمة | نعم | نعم | نعم | نعم | نعم |
| انقطاع أثناء التطبيق | نعم | نعم | نعم | نعم | نعم |
| تشفير الملف | نعم | نعم | نعم | نعم | نعم |
| تقرير التدقيق | نعم | نعم | نعم | نعم | نعم |

لا تعتبر الميزة مكتملة إذا نجحت في زوج واحد فقط.

---

## 19. التوثيق المطلوب كجزء من المنتج

يجب أن يبقى التوثيق في المستودع ويحدث مع كل مرحلة:

### وثائق المستخدم

- دليل إنشاء نسخة.
- دليل استعادة نسخة.
- دليل المزامنة المباشرة.
- دليل المزامنة بالملف.
- دليل مراجعة التعارض.
- دليل تغيير الهاتف أو جهاز Windows.

### وثائق المسؤول

- تشغيل النسخ المجدول.
- سياسة الاحتفاظ.
- استعادة الكوارث.
- إدارة العقد الموثوقة.
- إلغاء جهاز مفقود.
- مراجعة سجل التدقيق.
- تشخيص جلسة عالقة.

### وثائق المطور

- Canonical Data Contract.
- Node Identity.
- Change Log.
- Sync Protocol.
- Package Format.
- Conflict Rules.
- Migration Guide.
- OpenAPI.
- Database Migration Guide.

### توثيق المرحلة

كل مرحلة يجب أن تضيف:

```text
docs/
  phase-N-implementation-notes.md
  phase-N-test-report.md
  phase-N-operations.md
```

أو تدمج هذه الأقسام في هذه الوثيقة مع الحفاظ على سجل الإصدارات.

---

## 20. المراقبة والتدقيق

يجب تسجيل:

- إنشاء نسخة.
- فحص نسخة.
- استعادة.
- تراجع.
- إنشاء جلسة.
- اقتران عقدة.
- رفع حزمة.
- تنزيل حزمة.
- تطبيق حزمة.
- فشل تطبيق.
- تعارض.
- قرار حل تعارض.
- إلغاء عقدة.

كل سجل يجب أن يحتوي على:

```text
actor
nodeId
sessionId
packageId
action
result
createdAt
errorCode
metadata
```

يجب ألا يحتوي سجل التدقيق على:

- كلمات المرور.
- مفاتيح التشفير.
- Payload حساس غير ضروري.
- ملفات النسخ نفسها.

---

## 21. إدارة الإصدارات والتوافق

يجب فصل:

```text
appVersion
schemaVersion
syncProtocolVersion
backupFormatVersion
```

### قواعد التوافق

- إصدار التطبيق لا يعني بالضرورة توافق صيغة المزامنة.
- كل تغيير في صيغة الحزمة يحتاج Migration أو رفضاً واضحاً.
- لا تتم ترقية مخطط قاعدة البيانات دون Restore Point.
- لا يتم حذف محول إصدار قديم قبل انتهاء فترة الاحتفاظ بالصيغ القديمة.
- يجب دعم قراءة نسخة أقدم ضمن فترة معلنة.
- يجب رفض الكتابة إلى إصدار أقدم إذا كان ذلك قد يسبب فقدان حقول.

---

## 22. الأداء والحدود التشغيلية

يجب قياس وتوثيق:

- زمن إنشاء Full Backup.
- زمن إنشاء Delta.
- زمن التحقق.
- زمن Preview.
- زمن تطبيق الحزمة.
- حجم الذاكرة.
- حجم التخزين.
- عدد الحركات المدعوم في الجلسة.
- سلوك Android عند الملفات الكبيرة.

لا يجوز الاعتماد على حد ثابت مثل 2MB للحزم الكبيرة. يجب استخدام:

- رفع Streaming.
- تقسيم الحزمة إلى أجزاء.
- Checkpoint لكل جزء.
- استكمال الجزء المفقود.
- حد قابل للضبط حسب المنصة.

---

## 23. التراجع وخطة الطوارئ

### قبل أي تغيير

- Full Backup.
- التحقق من البصمة.
- اختبار قراءة النسخة.
- تسجيل الإصدار.
- إيقاف العمليات الحساسة عند الحاجة.

### عند فشل المزامنة

1. عدم حذف Outbox.
2. حفظ تقرير الفشل.
3. إبقاء الجلسة قابلة للاستكمال.
4. عدم إعادة إنشاء Operation IDs.
5. إعادة المحاولة بعد فحص السبب.
6. تحويل التعارض إلى المراجعة عند الحاجة.

### عند فساد قاعدة

1. إيقاف العقدة المتضررة.
2. حفظ نسخة من الحالة المتضررة للتحليل.
3. اختيار آخر Restore Point سليم.
4. استعادة Full Backup.
5. تطبيق Delta غير المطبق.
6. تشغيل اختبارات الأرصدة.
7. إنشاء تقرير حادثة.
8. إعادة تفعيل المزامنة بعد اعتماد مسؤول البيانات.

---

## 24. معايير القبول النهائية

لا تعتبر الميزة جاهزة للإنتاج إلا إذا تحققت كل النقاط التالية:

### الاستقلالية

- تعمل كل نسخة بقاعدة منفصلة.
- لا تحتاج النسخة المحلية إلى الخادم لإنشاء حركة.
- لا يتم تبادل البيانات دون إجراء مزامنة واضح.

### المزامنة

- تعمل كل الأزواج الثلاثة.
- تعمل ثنائية الاتجاه.
- تعمل مباشرة ويدوياً.
- لا تكرر العمليات.
- تستكمل بعد الانقطاع.
- تمرر التغييرات المستلمة إلى عقدة ثالثة.
- تعرض التعارضات.

### النسخ

- Full Backup قابل للقراءة والاستعادة.
- Delta Backup قابل للتطبيق بالترتيب.
- الملف مشفر وموقع أو قابل للتحقق.
- لا يحتوي أسراراً.
- توجد سياسة احتفاظ.
- توجد نسخة قبلية.

### الاستعادة

- Preview.
- Dry Run.
- Full Restore.
- Safe Merge.
- تقرير مفصل.
- Rollback.
- فحص أرصدة بعد التطبيق.

### المخزون

- صحة الرصيد.
- صحة FEFO.
- صحة الدفعات.
- صحة العهد.
- صحة التلف والمرتجع.
- صحة التجهيزات المرقمة.

### التشغيل

- توثيق المستخدم.
- توثيق المسؤول.
- توثيق المطور.
- تقارير اختبار لكل مرحلة.
- تقرير Pilot.
- قرار Go / No-Go.

---

## 25. ترتيب التنفيذ المختصر

لا يجوز عكس هذا الترتيب:

```text
0. اعتماد المتطلبات
1. جرد النموذج وقواعد المخزون
2. الهوية العالمية
3. Change Log وOutbox وInbox
4. صيغة الحزمة والتشفير
5. محرك الاستعادة
6. النسخ الكامل والتفاضلي
7. محرك المزامنة الثنائية
8. المزامنة اليدوية وRelay
9. التعارضات
10. تكامل Windows
11. تكامل Android
12. تكامل Web
13. ترحيل البيانات الحالية
14. التحمل والأمن والتعافي
15. التشغيل التجريبي والإطلاق
```

السبب في هذا الترتيب هو أن واجهة المزامنة لا يمكن أن تكون آمنة قبل وجود هوية عالمية وسجل تغييرات وصيغة موحدة. كما لا يمكن إطلاق الاستعادة قبل وجود Preview وRestore Point وفحص أرصدة.

---

## 26. الخلاصة

المنتج النهائي المطلوب هو نظام:

```text
Multi-Database
Offline-First
Bidirectional Sync
Conflict-Aware
Encrypted Backup
Transactional Restore
Auditable
Recoverable
```

ويجب أن يشعر المستخدم أن العملية بسيطة:

```text
مزامنة الآن
→ مراجعة الملخص
→ اعتماد
→ تقرير نجاح
```

لكن وراء هذا الزر يجب أن يعمل:

- معرف عقدة ثابت.
- معرف عالمي لكل سجل.
- سجل تغييرات Append-only.
- Vector Clock.
- حزمة معيارية مشفرة.
- تطبيق Idempotent.
- معالجة تعارضات.
- نسخة قبلية.
- Transaction.
- فحص أرصدة.
- تقرير تدقيق.

بهذا التصميم تعمل نسخة Windows ونسخة Android ونسخة الويب كل منها بصورة مستقلة، ويمكن نقل التغييرات بينها بأبسط تجربة استخدام ممكنة، دون التضحية بسلامة المخزون أو قابلية الاستعادة أو التتبع الإداري.

---

## 27. تقرير التحقق من تنفيذ المراحل 1–5

### 27.1 نطاق التحقق وحالة البوابة

تمت مراجعة التنفيذ الفعلي في المستودع، وتطبيق مخطط التطوير على قاعدة التطوير،
ثم تشغيل اختبارات المراحل 1–5. النتيجة: **المراحل 1–5 مجتازة في نطاق Web /
PostgreSQL والمكتبة المعيارية المشتركة**. لا يعني ذلك أن تكامل Windows وAndroid
النهائي مكتمل؛ تكامل المنصات مخصص للمراحل 10–12 كما هو موضح في هذه الوثيقة.

### 27.2 المرحلة 1 — جرد النموذج وقواعد المخزون

تم تثبيت خط أساس قابل للاختبار للنسخ الحالية:

| المنصة | مخزن البيانات المفحوص | نتيجة الجرد |
|---|---|---|
| Web | PostgreSQL + Drizzle | مخطط المواد والدفعات والحركات والعهد والتلف والمرتجع وقواعد القيود |
| Windows | PGlite | مخطط محلي متوافق مع الجداول التشغيلية وطبقة الهوية والسجل |
| Android | IndexedDB | حالة محلية مع عقد المزامنة المنطقية في `offline-api.ts` |

تم اختبار قيود الرصيد، الدفعات، التواريخ، أرقام السندات، مراجع الكيانات،
FEFO، الإدخال متعدد الدفعات، المعدات المسلسلة، العهد، التلف والمرتجع. بقيت
السجلات القديمة محفوظة، ومنحت هوية ربط مستقرة دون اختراع أحداث تاريخية.

### 27.3 المرحلة 2 — هوية العقد والسجلات العالمية

تم التحقق من `node_identity` و`sync_entity_ids` وحقول الحركات
`operationId` و`originNodeId` و`originSequence` و`documentNumberScope`.
يُحجز التسلسل داخل معاملة الحركة، وتبقى هوية العقدة ثابتة بعد إعادة التشغيل،
ولا تعاد معرّفات العمليات عند فشل الطلب أو إعادة المحاولة.

### 27.4 المرحلة 3 — Change Log وOutbox وInbox

تم التحقق من المخازن `sync_change_log` و`sync_outbox` و`sync_inbox`
و`sync_cursors` و`sync_conflicts` و`sync_tombstones`. الاختبارات أثبتت
الكتابة الذرية، بقاء Outbox، منع تكرار Inbox، منع عودة الحذف المنطقي، والتراجع
عن الكتابة الجزئية.

### 27.5 المرحلة 4 — الصيغة المعيارية والتشفير

تم اعتماد امتداد `.dme-sync` ومكتبة `@workspace/backup-format` التي تنشئ وتقرأ
حزم JSONL مضغوطة ومشفرة بـ AES-256-GCM ومحمية بـ HMAC-SHA-256 مع scrypt.
تتحقق المكتبة من البصمة، الإصدار، عدد السجلات، الحقول الحساسة، وكلمة المرور،
وترفض الحزم المعدلة أو الناقصة. استبعدت الحزم كلمات مرور المستخدمين والجلسات
والأسرار.

### 27.6 المرحلة 5 — محرك الاستعادة

تم تنفيذ الفحص والمعاينة وDry Run قبل التطبيق، والاستعادة الكاملة والدمج الآمن،
وتقرير نتيجة لكل سجل، وتطبيق ذري داخل معاملة، ونقطة استعادة قبلية مع Rollback.
تُرفض أرصدة المواد والدفعات غير المنطقية، ويظل دمج السجلات مكرراً آمناً
Idempotent. لا يمكن اعتماد Restore دون `previewToken` و`confirm=true`.

### 27.7 سجل اختبارات المراحل 1–5

تم تنفيذ الأوامر التالية على قاعدة التطوير بعد تطبيق المخطط:

| الاختبار | النتيجة |
|---|---|
| `pnpm --filter @workspace/scripts run phase1:schema-smoke` | PASS — 7 قيود ومراجع ومخزون |
| `pnpm --filter @workspace/scripts run phase2:movement-unit` | PASS — 7 حالات FEFO والتحقق |
| `pnpm --filter @workspace/scripts run phase2:movement-smoke` | PASS — 6 سيناريوهات ذرية وتزامنية |
| `pnpm --filter @workspace/scripts run phase123:sync-smoke` | PASS — الهوية والذرية وOutbox وInbox وTombstone |
| `pnpm --filter @workspace/scripts run phase3:inbound-smoke` | PASS — 4 حالات إدخال |
| `pnpm --filter @workspace/scripts run phase4:consumables-fefo` | PASS — 6 حالات FEFO |
| `pnpm --filter @workspace/scripts run phase5:assets-lifecycle` | PASS — 7 حالات دورة المعدات والعهد |
| `pnpm --filter @workspace/scripts run phase45:backup-recovery` | PASS — التشفير والعبث وكلمة المرور والحقول الحساسة |
| `pnpm --filter @workspace/scripts run phase45:db-smoke` | PASS — الدمج والتكرار ورفض الرصيد السالب |
| `pnpm --filter @workspace/scripts run phase6:item-history` | PASS — 4 حالات بطاقة المادة والسجل |
| `pnpm --filter @workspace/db run push-force` | PASS — مخطط التطوير |

### 27.8 ملاحظة بيئية تم علاجها

في أول تشغيل كانت قاعدة التطوير تفتقد جداول المرحلة 3، فأوقف الاختبار بسبب
`sync_tombstones does not exist`. تم تطبيق مخطط التطوير ثم إعادة تشغيل المجموعة
كاملة بنجاح. هذا ليس تجاهلاً لفشل اختبار؛ بل تم توثيق السبب والإجراء والنتيجة.

## 28. تقرير تنفيذ المرحلة 6 — النسخ التلقائي والنسخ التفاضلي

### 28.1 التنفيذ

تم تنفيذ العناصر التالية:

- إنشاء Full Backup مشفر من البيانات التشغيلية وسجل التغييرات.
- إنشاء Delta Backup من التغييرات التي تتجاوز `baseVector`، مع `lastVector`
  وسجلات الحالة الحالية للكيانات المتأثرة حتى يمكن تطبيق Delta بعد خط الأساس.
- إضافة `backup_catalog` لحفظ الحزمة المشفرة وبياناتها الوصفية دون كلمة المرور.
- إضافة `backup_retention_policy` بسياسة يومية/أسبوعية/شهرية قابلة للتعديل.
- إضافة فحص دوري للبصمة مع عدم اعتبار كلمة المرور الخاطئة تلفاً للنسخة.
- إضافة تطبيق الاحتفاظ مع حماية آخر Full Backup سليم.
- إضافة جدولة Web اختيارية، تبدأ بـFull ثم تستخدم Delta من آخر متجه معروف.
- إضافة مسارات الكتالوج والتنزيل والتحقق وإدارة السياسة، مع بقاء المسارات
  القديمة للاستعادة والتوافق.
- إضافة اختبار قاعدة بيانات مستقل للمرحلة السادسة مع تنظيف كامل لبياناته.

### 28.2 الواجهات التشغيلية

| المسار | الوظيفة |
|---|---|
| `GET /api/backups` | كتالوج النسخ وسياسة الاحتفاظ |
| `POST /api/backups` | إنشاء Full أو Delta وحفظها في الكتالوج |
| `GET /api/backups/:backupId/package` | تنزيل الحزمة المشفرة |
| `POST /api/backups/:backupId/verify` | فحص كلمة المرور والبصمة |
| `GET/PUT /api/backups/policy` | قراءة وتحديث حدود الاحتفاظ |
| `POST /api/backups/retention/enforce` | تطبيق الاحتفاظ يدوياً |

الجدولة مغلقة افتراضياً. لتفعيلها يجب حفظ `BACKUP_SCHEDULER_PASSWORD` في
Secrets وضبط `BACKUP_SCHEDULER_ENABLED=1`، ويمكن ضبط الفاصل عبر
`BACKUP_SCHEDULE_INTERVAL_MS`. عند غياب كلمة مرور الجدولة لا يبدأ المؤقت، ولا
تُحذف النسخ السابقة عند فشل إنشاء نسخة جديدة.

### 28.3 اختبار المرحلة 6

تم تنفيذ:

```bash
pnpm --filter @workspace/scripts run phase6:backup
```

والنتيجة:

| السيناريو | النتيجة |
|---|---|
| Full Backup مشفر ومدرج في الكتالوج | PASS |
| تغييرات معلقة في Change Log وOutbox | PASS |
| Delta يتجاوز `baseVector` ويحتوي السجل المتأثر | PASS |
| تطبيق Delta بعد خط أساس Full | PASS |
| مطابقة البصمة والتحقق الدوري | PASS |
| الاحتفاظ بعدد النسخ مع حماية آخر Full سليم | PASS |

### 28.4 بوابة المرحلة والحدود المعلنة

بوابة المرحلة السادسة مجتازة في نطاق خادم Web وقاعدة PostgreSQL. واجهة Web
تُنزل ملف `.dme-sync`، أما اختيار مسار الحفظ المحلي في Windows وAndroid فيبقى
ضمن محولات المنصة في المرحلتين 10 و11؛ لم يتم الادعاء بتنفيذ تلك المحولات هنا.
كما أن Relay والمزامنة الثنائية والتعارضات المتقدمة ليست جزءاً من المرحلة 6
وتبقى للمرحلة 7 وما بعدها.

## 29. حالة GitHub وريبليت

تم ربط مساحة عمل Replit بالمستودع الحالي:
`https://github.com/ibrahims78/Damascus-Health-Directorate.git` على الفرع
`master`، وضُبط `origin` عبر HTTPS مع تتبع `origin/master`. القراءة والدفع
يعملان دون OAuth عبر رمز وصول محفوظ في Replit Secrets، ولا يوضع الرمز في
عنوان remote أو الملفات أو المحادثة. بعد كل رفع يجب فحص تطابق `HEAD` مع
`origin/master`.

## 30. تقرير التحقق النهائي للمراحل 1–6

### 30.1 نطاق التحقق

في 18 آب 2026 تم تشغيل الاختبارات على قاعدة التطوير PostgreSQL بعد تهيئة
المخطط من تعريف Drizzle، ثم تطبيق اختبارات كل مرحلة بالتتابع. هذا يثبت بوابات
المراحل 1–6 في نطاق Web/PostgreSQL والمكتبة المعيارية المشتركة. لا يدعي هذا
التقرير اكتمال محولات Windows وAndroid النهائية، إذ إن تكاملها الرسمي موجود في
المراحل 10 و11.

### 30.2 سجل التنفيذ والنتيجة

| المرحلة | أمر التحقق | النتيجة |
|---|---|---|
| 1 | `pnpm --filter @workspace/scripts run phase1:schema-smoke` | PASS — 7 قيود ومراجع وقواعد سلامة |
| 2 | `phase2:movement-unit` و`phase2:movement-smoke` | PASS — 7 حالات وحدات و6 سيناريوهات ذرية وتزامنية |
| 3 | `phase123:sync-smoke` و`phase3:inbound-smoke` | PASS — الهوية وChange Log وOutbox وInbox وTombstone و4 إدخالات |
| 4 | `phase45:backup-recovery` و`phase4:consumables-fefo` | PASS — 6 اختبارات صيغة/تشفير و6 اختبارات FEFO |
| 5 | `phase45:db-smoke` و`phase5:assets-lifecycle` | PASS — الدمج والتكرار ورفض الرصيد السالب و7 حالات دورة المعدات |
| 6 | `phase6:item-history` و`phase6:backup` | PASS — 4 حالات تاريخ المادة وFull/Delta/البصمة/الاحتفاظ |

كما نجح:

```bash
pnpm run typecheck
```

بكل حزم workspace، ونجحت تهيئة قاعدة التطوير باستخدام:

```bash
pnpm --filter @workspace/db run push-force
```

بعد التحقق من أن `users` و`sync_change_log` و`sync_cursors` و`backup_catalog`
والجداول الجديدة موجودة. لم تُستخدم تهيئة أو تعديل لقاعدة الإنتاج.

### 30.3 بوابة المراحل 1–6

بوابة المراحل 1–6 **مجتازة** ضمن النطاق المعلن: قواعد المخزون قابلة للاختبار،
الهوية العالمية وسجل التغييرات ذريان، الصيغة `.dme-sync` مشفرة وقابلة للتحقق،
الاستعادة تسبقها معاينة ونقطة تراجع، والنسخ Full/Delta لها كتالوج وبصمة
واحتفاظ. حدود Windows/Android وRelay وحل التعارضات المتقدمة بقيت معلنة ولم
تُحسب نجاحاً زائفاً لهذه المراحل.

## 31. تقرير تنفيذ المرحلة 7 — المزامنة الثنائية الأساسية

### 31.1 التنفيذ

تم تنفيذ المرحلة 7 فوق طبقات الهوية وسجل التغييرات القائمة دون تغيير معادلات
المخزون:

- إنشاء `sync_sessions` لجلسة ثنائية دائمة قابلة للاستكمال والانتهاء.
- إنشاء `sync_session_packages` لحفظ اتجاه الحزمة، المتجهين، المحتوى، البصمة،
  الحالة، والتقرير.
- إضافة `GET /api/sync/node` لإرجاع هوية العقدة والمتجه الحالي.
- إضافة `POST /api/sync/sessions` و`GET /api/sync/sessions/:sessionId`.
- إضافة `POST .../handshake` لتبادل Vector Clock.
- إضافة `POST .../manifest` لإنشاء Delta من التغييرات الأعلى من `baseVector`.
- إضافة `POST .../packages` للتحقق من البصمة والفجوات وتطبيق Inbox/Change Log
  داخل معاملة ذرية.
- إضافة `GET .../packages/:packageId` لاسترجاع الحزمة المحفوظة.
- إضافة `POST .../ack` لتأكيد الاستلام وتحديث Outbox وحالة الجلسة.
- منع إعادة تطبيق `operationId` أو `packageId`، وحفظ التعارض الدلالي الواضح
  بدلاً من تطبيقه بصمت.
- رفض Gap التسلسلي قبل أي كتابة، وحفظ Cursor للند حتى يمكن استكمال التبادل.

### 31.2 بروتوكول التشغيل

```text
العقدة A: إنشاء Session
→ تبادل Vector Clock عبر Handshake
→ إنشاء Manifest/Delta من A إلى B
→ نقل الحزمة إلى B
العقدة B: تحقق البصمة والتسلسل
→ تطبيق ذري إلى Inbox وChange Log
→ إعادة ACK
العقدة A: تحديث Outbox وSession Report
→ تكرار الاتجاه B إلى A حتى اكتمال الاتجاهين
```

مرحلة 7 تطبق السجل المعياري وInbox/Outbox وCursor، ولا تدعي بعد تطبيق
الإسقاطات الخاصة بكل منصة أو حل تعارضات المخزون المعقدة؛ ذلك مرتبط بمحولات
المنصات وقواعد المرحلة 9.

### 31.3 اختبار قبول المرحلة 7

تم تنفيذ:

```bash
pnpm --filter @workspace/scripts run phase7:sync
```

والنتيجة:

| السيناريو | النتيجة |
|---|---|
| A إلى B: Delta بعد المتجه المتبادل | PASS |
| إعادة طلب Manifest نفسه | PASS — نفس الحزمة دون تكرار |
| تطبيق الحزمة داخل Inbox/Change Log | PASS — ذري |
| إرسال الحزمة نفسها مرتين | PASS — لا تكرار |
| وجود Gap في التسلسل | PASS — رفض بلا كتابة جزئية |
| وصول الجزء المفقود واستكمال الجلسة | PASS |
| ACK وتحديث Outbox وتقرير الجلسة | PASS |

بوابة المرحلة السابعة **مجتازة** في نطاق المحرك الأساسي Web/PostgreSQL. أما
الاتصال المباشر بين تطبيقات Windows وAndroid وRelay، وواجهة المزامنة اليدوية،
فتبقى ضمن المراحل 8 و10 و11 و12 كما يحدد ترتيب التنفيذ.

## 32. تقرير تنفيذ المراحل المتبقية — 2026-08-18

### 32.1 المرحلة 8 — المزامنة اليدوية وRelay

تم تنفيذ عقد Relay على الخادم مع إبقاء محتوى الحزمة مشفراً وغير قابل للقراءة
من الخادم:

- إضافة `sync_pairings` لرموز اقتران عشوائية، قصيرة العمر، أحادية الاستخدام.
- إضافة `sync_trusted_nodes` مع حالات `trusted` و`revoked` دون حذف سجل الثقة.
- إضافة `sync_relay_packages` لتخزين Base64 opaque وبصمة نقل SHA-256، مع انتهاء
  صلاحية وفهرسة حسب الجلسة. حد الحمولة 64 MiB.
- إضافة مسارات pairing والعقد الموثوقة ورفع/تنزيل/قائمة/تنظيف حزم Relay.
- رفض الوجهة والاتجاه والجلسة غير المطابقة قبل الحفظ، وإعادة نفس Relay item
  عند إعادة رفع البصمة نفسها.
- إضافة صفحة **المزامنة والعقد** لإدارة الهوية والاقتران وRelay.

اختبار القبول:

```bash
pnpm --filter @workspace/scripts run phase8:relay
```

النتيجة: **PASS — 4 فحوص**: pairing أحادي الاستخدام، تخزين/تنزيل opaque
idempotent، رفض عقدة خاطئة، واستمرار الجلسة بعد إلغاء الثقة. بوابة المرحلة 8
مجتازة ضمن عقد Relay والملف؛ تطبيقات Windows وAndroid تستهلك العقد نفسه.

### 32.2 المرحلة 9 — التعارضات والتسويات

تم تحويل جدول التعارض الموجود من سجل شكلي إلى طابور قابل للمراجعة:

- تصنيف الخطورة `low/medium/high/critical`، مع اعتبار الرصيد والعهدة
  `critical` وحذف السجلات أو تعارض المستندات `high`.
- إضافة API لاستعراض التعارضات حسب الحالة، وقرارات اعتماد/رفض/تصحيح/تأجيل.
- تسجيل المستخدم والوقت والقرار، وإنشاء Change من نوع `correction` عند التصحيح.
- إضافة أزرار القرار إلى صفحة الإدارة، مع منع التطبيق الصامت للتعارض.

اختبار القبول:

```bash
pnpm --filter @workspace/scripts run phase9:conflicts
```

النتيجة: **PASS — فحصان**: سياسة الخطورة وطابور القرار/التأجيل. بوابة
المرحلة 9 مجتازة للقرارات المعيارية؛ سيناريوهات الرصيد الفعلية تظل مربوطة
بمحرك الحركة وتُرفض تلقائياً عند عدم تحقق invariant.

### 32.3 المرحلة 10 — Windows والمرحلة 11 — Android Offline

تم تثبيت العقد المشترك `@workspace/sync-contract` ليكون المصدر نفسه لهوية
العقدة والمتجه والتغييرات والتحقق قبل الاستيراد. وضع سطح المكتب يستخدم PGlite
و`desktop-schema.sql`، ووضع Android يستخدم IndexedDB مع `nodeIdentity` و
`changeLog/outbox/inbox/conflictQueue`، كما أن صيغة `.dme-sync` والتشفير
مشتركان على مستوى النقل.

اختبار العقد المشترك:

```bash
pnpm --filter @workspace/scripts run phase10:13:portability
```

النتيجة: **PASS — فحصان**: اختلاف هويات Windows/Android، حساب الفروقات، ورفض
هوية تغيير غير صالحة قبل الاستيراد. لا تُعتبر هذه النتيجة اختباراً لجهاز مادي
أو فقد طاقة؛ اختبار الأجهزة الفعلية يبقى بند Pilot في المرحلة 15.

### 32.4 المرحلة 12 — Web Server والإدارة

تم ربط API المرحلة 7 وRelay والتعارضات تحت `/api/sync` مع صلاحية admin وتسجيل
التدقيق، وإضافة صفحة الإدارة `/sync` وقائمة العقد الموثوقة وطابور التعارضات.
النسخ والاستعادة المركزية الموجودة في `/api/backups` تبقى منفصلة عن
المزامنة كما تنص المواصفة.

### 32.5 المرحلة 13 — الترحيل

أضيفت أداة قراءة baseline آمنة لا تطبق أي دمج تلقائياً:

```bash
pnpm --filter @workspace/scripts run migration:baseline-report \
  -- --input legacy.json --output baseline-report.json
```

تنتج الأداة عدادات الجداول، ربط `localId → globalId`، التكرارات، الحقول
الحساسة والتحذيرات، وتمنع اعتبار التقرير تطبيقاً ناجحاً. لا تُقبل بيانات حقيقية
قبل مراجعة التقرير واعتماد مسؤول البيانات.

### 32.6 المرحلة 14 — التحمل والأمن والتعافي

أضيف اختبار حزمة كبير قابل للتكرار:

```bash
pnpm --filter @workspace/scripts run phase14:resilience
```

النتيجة المسجلة: **PASS** لإنشاء واستعادة 100,000 سجل و10,000 تغيير، و**PASS**
لرفض العبث وكلمة المرور الخاطئة دون تطبيق البيانات. حد Relay الحالي 64 MiB،
وحد الحزمة المشفرة 512 MiB؛ يجب اعتماد حدود أقل أو أعلى في Pilot بناءً على
مساحة الأجهزة الفعلية.

### 32.7 المرحلة 15 — التشغيل التجريبي والإطلاق

تم إنشاء `docs/pilot-runbook-ar.md` بدورة يومية، فقد جهاز، جهاز جديد، فقد ACK،
مؤشرات المتابعة، ونموذج Go/No-Go. لا تُعلن هذه المرحلة مكتملة بمجرد وجود
الدليل؛ اعتمادها يتطلب تنفيذ الدورة على عقد فعلية وتوقيع مسؤول البيانات.

### 32.8 بوابة الجودة المشتركة

الأوامر التي اجتازت التحقق البرمجي في هذه الدورة:

```bash
pnpm run typecheck:libs
pnpm --filter @workspace/api-server typecheck
pnpm --filter @workspace/web typecheck
pnpm --filter @workspace/scripts typecheck
pnpm --filter @workspace/scripts run phase7:sync
pnpm --filter @workspace/scripts run phase8:relay
pnpm --filter @workspace/scripts run phase9:conflicts
pnpm --filter @workspace/scripts run phase10:13:portability
pnpm --filter @workspace/scripts run phase14:resilience
```

كلها **PASS** في بيئة التطوير مع PostgreSQL. لم تُطبق أي تغييرات على قاعدة
الإنتاج، ولم تُدرج أسرار أو رموز وصول في Git. تبقى موافقة Pilot الفعلية
والرفع إلى GitHub بعد توفير رمز الوصول الآمن بندي تشغيل خارجيين، وليسا سبباً
لإخفاء حدود الاختبارات الحالية.

## 33. تقرير التحقق الإضافي — 2026-08-18

### 33.1 الفجوات التي أُغلقت

أثناء إعادة التحقق من المراحل المتبقية تبين أن تقرير التنفيذ السابق كان أوسع
من الحالة الفعلية في نقطتين، وتم تصحيحهما قبل إعلان الجاهزية:

1. عقود النسخ والاستعادة والمزامنة وRelay والتعارضات لم تكن ممثلة في
   `lib/api-spec/openapi.yaml`. أضيفت المسارات والمدخلات الأساسية، ثم أعيد
   توليد عميل React وZod بنجاح.
2. أداة Baseline كانت تنشئ `globalId` عشوائياً لكل تشغيل. أصبحت الهوية الناتجة
   حتمية وفق نوع الكيان والمعرف المحلي، حتى تعطي إعادة الترحيل نفس خريطة
   `legacyId → globalId`، مع استمرار منع التطبيق التلقائي.

كما تم تأمين استهلاك رمز الاقتران ضد السباق: تتم المطالبة بالرمز داخل
`UPDATE ... RETURNING` مشروط قبل إنشاء العقدة الموثوقة، فلا يمكن لطلبين
متزامنين اعتماد الرمز نفسه.

### 33.2 اختبارات التحقق التي أُعيد تشغيلها

| المجال | الأمر | النتيجة |
|---|---|---|
| البناء والأنواع | `pnpm run typecheck` | **PASS** — مكتبات API والخادم والواجهة والاختبارات |
| عقد الإدارة | `pnpm --filter @workspace/scripts run phase12:admin-contract` | **PASS** — OpenAPI والمسارات والواجهات مترابطة |
| الترحيل | `pnpm --filter @workspace/scripts run phase13:migration` | **PASS** — خريطة حتمية، تكرارات وحقول حساسة معلنة |
| التحمل والأمن | `pnpm --filter @workspace/scripts run phase14:resilience` | **PASS** — 100,000 سجل و10,000 تغيير ورفض العبث |
| Relay والاقتران | `pnpm --filter @workspace/scripts run phase8:relay` | **PASS** — الاقتران المتزامن أحادي الاستهلاك |
| المزامنة الأساسية | `pnpm --filter @workspace/scripts run phase7:sync` | **PASS** — الحزم والفجوات وACK والاستكمال |
| التعارضات | `pnpm --filter @workspace/scripts run phase9:conflicts` | **PASS** — التصنيف وطابور القرار |
| عقود Windows/Android | `pnpm --filter @workspace/scripts run phase10:13:portability` | **PASS** — الهوية والمتجه والتحقق قبل الاستيراد |
| البناء الإنتاجي | `pnpm --filter @workspace/web run build` و`pnpm --filter @workspace/api-server run build` | **PASS** — التحذيرات مقتصرة على sourcemap وحجم الحزم |
| صحة الخادم | `curl http://127.0.0.1:8080/api/healthz` | **PASS** — `status: ok` |

وأعيد تشغيل مجموعة اختبارات المراحل 1–6 كاملة، وجميعها **PASS** كما هو موثق
في الأقسام السابقة. طبقت جداول التطوير على قاعدة PostgreSQL التطويرية فقط؛
لم تُقرأ أو تُعدّل قاعدة الإنتاج.

### 33.3 حالة البوابات

| المرحلة | الحالة الحالية |
|---|---|
| 0–14 | **مكتملة برمجياً ومختبرة** ضمن حدود Web/PostgreSQL والعقود المشتركة، مع توثيق القيود |
| 15 | **معلّقة تشغيلياً** حتى تنفيذ Pilot على عقد Windows وAndroid وWeb فعلية وتوقيع Go/No-Go |

لا يصح إعلان المرحلة 15 مكتملة من اختبار الخادم وحده؛ فهي تتطلب دورة تشغيل
فعلية، فقد جهاز، جهازاً جديداً، فقد ACK، ومطابقة أرصدة يراجعها مسؤول البيانات.
توجد خطوات التنفيذ ونموذج القرار في `docs/pilot-runbook-ar.md`.

### 33.4 حالة التطابق والرفع

عند بدء الدورة كان `HEAD` المحلي مطابقاً لـ`origin/main` على الفرع `main`،
والـremote مضبوطاً بعنوان HTTPS العام. بعد هذه التحسينات أصبح لدى النسخة المحلية
تغييرات جديدة مختبرة تنتظر دفعاً مصادقاً. لا يمكن إعلان التطابق النهائي مع
المستودع قبل توفير `GITHUB_TOKEN` عبر Replit Secrets وتنفيذ:

```bash
git push origin main
git fetch origin main
test "$(git rev-parse HEAD)" = "$(git rev-parse origin/main)"
```

بعد نجاح هذه الخطوة تصبح نسخة Replit و`origin/main` متطابقتين وجاهزتين لرفع
التعديلات اللاحقة عبر HTTPS دون OAuth.

## 34. تقرير دورة التحقق الحالية — 2026-08-18

### 34.1 نطاق العمل المنفذ

تمت مراجعة المشروع بعد استنساخه من المستودع العام على الفرع `main`، ثم تثبيت
التبعيات من `pnpm-lock.yaml` دون تغيير الإصدارات. كان التنفيذ البرمجي للمراحل
1–14 موجوداً في نقطة المصدر الحالية، لذلك ركزت هذه الدورة على إعادة التحقق
الفعلية، وإغلاق فجوة تهيئة قاعدة التطوير، وتحديث هذا التقرير. لم تُجرَ أي
تغييرات على قاعدة الإنتاج.

### 34.2 نتيجة المرحلة 1

بوابة المرحلة 1 **مجتازة**. تم التحقق من مخطط PostgreSQL وقواعد المخزون
والحركات والعهد والتجهيزات والدفعات والمراجع، مع نجاح اختبار القيود التالية:

- عدم قبول كميات الدفعات السالبة أو رصيد متبقٍ أكبر من الكمية المستلمة.
- عدم قبول مصدر توريد غير مركزي أو تاريخ تقويمي غير صحيح.
- منع تكرار رقم السند.
- منع إرجاع عهدة بكمية أكبر من المسلّمة.
- منع خلط نوع الكيان بين الحركة والمرتجع.

أمر الاختبار:

```bash
pnpm --filter @workspace/scripts run phase1:schema-smoke
```

النتيجة: **PASS — 7 فحوص**.

### 34.3 الاختبارات الشاملة للمراحل 1–14

في أول تشغيل توقفت اختبارات قاعدة البيانات لأن قاعدة التطوير لم تكن مهيأة
بجداول `users` و`sync_tombstones`. تم علاج ذلك بتطبيق مخطط Drizzle على قاعدة
التطوير فقط:

```bash
pnpm --filter @workspace/db run push-force
```

ثم أعيد تشغيل المجموعة كاملة، وكانت النتائج:

| المجال | الأمر أو النطاق | النتيجة |
|---|---|---|
| الأنواع | `pnpm run typecheck` | **PASS** |
| المرحلة 1 | `phase1:schema-smoke` | **PASS — 7 فحوص** |
| المرحلة 2 | اختبارات الحركة والوحدة والتزامن | **PASS — 7 وحدات و6 سيناريوهات** |
| المرحلة 3 | Change Log وOutbox وInbox والإدخال | **PASS** |
| المرحلتان 4–5 | FEFO ودورة التجهيزات والنسخ والاستعادة | **PASS** |
| المرحلة 6 | تاريخ المادة والنسخ Full/Delta والاحتفاظ | **PASS** |
| المرحلة 7 | المزامنة الثنائية والفجوات وACK والاستكمال | **PASS** |
| المرحلة 8 | Relay والاقتران أحادي الاستخدام | **PASS** |
| المرحلة 9 | تصنيف التعارضات وطابور القرار | **PASS** |
| المرحلتان 10–11 | عقد Windows/Android المشترك والتحقق قبل الاستيراد | **PASS** |
| المرحلة 12 | عقد الإدارة وOpenAPI | **PASS** |
| المرحلة 13 | خريطة الترحيل الحتمية والتحذيرات | **PASS** |
| المرحلة 14 | 100,000 سجل و10,000 تغيير ورفض العبث | **PASS** |
| البناء | `@workspace/web` و`@workspace/api-server` | **PASS** |

### 34.4 التحقق التشغيلي

تمت إعادة تشغيل workflow للخادم والواجهة بنجاح:

```text
API:      http://127.0.0.1:8080/api/healthz → {"status":"ok"}
Web:      http://127.0.0.1:22333/ → HTTP 200
```

فتحت الواجهة شاشة إعداد حساب المدير بصورة صحيحة. ظهرت استجابات `401` في
console لطلبات محمية قبل تسجيل الدخول، وهذا متوقع في شاشة الإعداد الأولي
ولا يمثل فشلًا في تشغيل الخادم أو الواجهة.

### 34.5 حالة البوابات

| المرحلة | الحالة بعد هذه الدورة |
|---|---|
| 0–14 | **مكتملة برمجياً ومختبرة** ضمن نطاق Web/PostgreSQL والعقود المشتركة |
| 15 | **معلقة تشغيلياً** حتى تنفيذ Pilot فعلي على Windows وAndroid وWeb، واختبار فقد جهاز وفقد ACK ومطابقة الأرصدة، ثم اعتماد Go/No-Go من مسؤول البيانات |

لا يجوز تحويل المرحلة 15 إلى **مكتملة** اعتماداً على اختبارات الخادم فقط؛
تفاصيل التنفيذ ونموذج الاعتماد موجودان في `docs/pilot-runbook-ar.md`.

### 34.6 حالة المستودع والرفع

تم التأكد من أن:

```text
branch: main
remote: https://github.com/ibrahims78/Damascus-Health-Directorate.git
branch: master
read/fetch/push: PASS عبر HTTPS دون OAuth
بعد تحديث هذا التقرير: HEAD == origin/master
```

هذا التحديث الوثائقي يُدفع إلى `origin/master` عبر HTTPS باستخدام
`GITHUB_TOKEN` المحفوظ في Replit Secrets. لن يُحفظ
الرمز في Git أو في `origin` أو في هذه الوثيقة. بعد الدفع يجب تنفيذ:

```bash
git fetch origin main
test "$(git rev-parse HEAD)" = "$(git rev-parse origin/main)"
```

وبذلك تكون نسخة Replit والمستودع متطابقين وجاهزين لرفع التعديلات اللاحقة.