# تقرير تطابق النسخ الثلاث (ويب / ويندوز / أندرويد)

الإصدار: **4.4.0** — تحديث: 2026-09-16 (بعد تنفيذ التطابق الكامل)

## الخلاصة

| المقارنة | النتيجة |
|---|---|
| **الواجهات والشاشات** | ✅ متطابقة تمامًا في النسخ الخمس المبنية |
| **نسخة الويب ↔ نسخة ويندوز** | ✅ تطابق كامل في الميزات والتقارير |
| **نسختا أندرويد ↔ الويب/ويندوز** | ✅ **تطابق كامل** بعد تنفيذ الوحدات السبع في الطبقة المحلية |

## 1) الواجهات — متطابقة
كل الحزم المبنية الخمس (ويب، ويندوز محمول، ويندوز محمي، أندرويد أوفلاين، أندرويد محمي)
تحتوي نفس حزم JS الـ72 ونفس الصفحات.

## 2) الويب ↔ ويندوز — تطابق كامل
- نفس حزمة الويب (وضع الخادم) ونفس خادم الـAPI (24 مجموعة مسارات / 120 مسارًا).
- حزمة ويندوز تُشغّل نفس الخادم محليًا؛ تم التحقق بتشغيل الحزمة فعليًا:
  `/api/healthz → 200` و`/api/auth/setup-status → 200`.

## 3) أندرويد — صار متطابقًا (ما كان ناقصًا وأُنجز)
نُفِّذت الوحدات التالية داخل `artifacts/web/src/lib/offline-api.ts`:

| # | الميزة | المسارات | الحالة |
|---|---|---|---|
| 1 | كتالوج الوحدات | `/api/units` · `/usage` · `/seed-defaults` · `/normalize` · CRUD | ✅ |
| 2 | المستودعات | `/api/warehouses` · `/current` · `/next-document-number` · CRUD | ✅ |
| 3 | التحويلات | `/api/transfers` (طلب/إرسال/استلام/رفض/إلغاء) + التفاصيل | ✅ |
| 4 | حاكمية الاستيراد | `/api/import-batches` + `/rollback` | ✅ |
| 5 | لوحة المزامنة | `/api/sync/overview` | ✅ |
| 6 | التقارير الجديدة | `reconciliation` · `stock-by-warehouse` · `consolidated` | ✅ |
| 7 | تصدير المواد | `/api/items/export` | ✅ |

بالإضافة إلى إصلاح جذري: الحركات على الجهاز كانت تُحدّث الرصيد فقط دون دفتر الدفعات؛
الآن كل إدخال يفتح دفعة، وكل إخراج يستهلك الدفعات (FEFO) مع تسجيل التخصيصات،
فصار سجل الدفعات مصدر الحقيقة على الهاتف كما هو على الخادم.

## 4) أدلة الاختبار
`docs/tests/offline-parity-test.mjs` — **44/44**، ويشمل:
- الوحدات: بذر الافتراضيات، منع التكرار (409 `UNIT_NAME_DUPLICATE`)، الاستخدام، التطبيع.
- المستودعات: مستودع مركزي افتراضي، إنشاء فرع، منع تكرار الرمز، تبديل المستودع الحالي،
  والترقيم `CODE-TYPE-YEAR-NNNNNN` (مثال: `B1-IN-2026-000001`).
- التحويلات: لا يمكن الاستلام قبل الإرسال (409 `INVALID_TRANSITION`)، الإرسال يخصم من المركزي (10 → 6)،
  الاستلام يُدخل إلى الفرع (4)، والترصيد يبقى مطابقًا (0 فروق).
- التقارير الثلاثة + حاكمية الاستيراد + لوحة المزامنة + تصدير المواد.

## 5) مراجعة معمّقة لاحقة (2026-09-16)
أُجريت مراجعة كود شاملة للطبقة المحلية وأُصلحت 8 مسائل — التفاصيل في
`docs/offline-parity-code-review-ar.md`.

## 6) التفعيل في النسخ المحمية — يعمل
- ويندوز المحمية: التفعيل عبر الخادم (`/api/license/*`) ✅
- أندرويد المحمية: التحقّق **محليًا** بالمفتاح العام المدمج (بالتصميم) ✅ — 10/10 في اختبار المجموعة.
