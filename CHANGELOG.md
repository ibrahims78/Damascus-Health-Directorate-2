# سجل التغييرات — CHANGELOG

## 2026-09-17 — v5.1 (تقييم المخزون FIFO ومحاسبة التكلفة)

- **تكلفة الوحدة** لكل صنف (`items.unit_cost`) ولكل دفعة (`inventory_batches.unit_cost`) ولكل بند في سند
  الاستلام (`receipt_lines.unit_cost`)، وتُسند الدفعة تكلفتها عند الإدخال (أو ترث افتراضي الصنف في التقارير).
- **تقرير تقييم المخزون** (`/api/reports/valuation`): قيمة المخزون بطريقة **FIFO** لكل صنف وإجمالًا،
  ومتوسط تكلفة الوحدة، وعدد الأصناف التي بها دفعات بلا تكلفة.
- **تقرير تكلفة المصروف** (`/api/reports/cogs`) على نافذة زمنية، مبني على طبقات الدفعات المستهلكة فعليًا (FEFO).
- اختبارات: **58/58** في الميزات المتقدمة (منها 10 فحوص تقييم: 7200 → 6000 والقيمة المصروفة 1200 = 100×12).

## 2026-09-16 — v5.0.2 (استيراد الكتالوج من CSV)

- لوحة **استيراد الكتالوج** تقبل الآن **CSV** إلى جانب Excel: يُقرأ كجدول مواد واحد بعناوينه العربية نفسها،
  مع تنبيه واضح بأن ورقة التجهيزات غير متاحة في CSV.
- عيّنة جاهزة: `fixtures/catalog-import/sample-catalog.csv` (إلى جانب عيّنتي Excel).
- بقية مسار الاستيراد كما هو: معاينة لا تكتب · رفض التنفيذ الجزئي · تقرير أخطاء CSV · حوكمة وتراجع.

## 2026-09-16 — v4.9.0 (2FA على الهاتف/الأوفلاين)

- طبقة الأوفلاين تنفّذ TOTP بـWebCrypto (HMAC-SHA1) بنفس معيار RFC، وأصبحت **تُلزم المصادقة الثنائية**
  عند تسجيل الدخول على الجهاز إذا كان الحساب مُفعّلًا.
- مسارات محلية: `/api/auth/2fa/setup|enable|disable|status|reset` على الجهاز أيضًا.
- أدلة: تطابق الأوفلاين **75/75** (منها 6 فحوص 2FA على الجهاز).

## 2026-09-16 — v4.9.0 (المصادقة الثنائية 2FA)

- **TOTP (RFC 6238) / HOTP (RFC 4226)** منفّذان على `node:crypto` بلا أي مكتبة خارجية،
  ومُتحقَّق منهما مقابل **متجهات الاختبار القياسية** (5/5 اختبارات وحدة).
- حقول `two_factor_secret`/`two_factor_enabled` على المستخدمين.
- مسارات: `/api/auth/2fa/setup` (إصدار مفتاح + otpauth) · `/enable` · `/disable` · `/status` ·
  و`/api/auth/2fa/reset` (استعادة حساب مدير عند فقدان الجهاز).
- **دخول بخطوتين**: كلمة المرور ثم الرمز؛ والرمز الخاطئ يُحتسب محاولة فاشلة. صفحة الدخول تعرض حقل الرمز تلقائيًا.
- اختبار تكامل: **12/12** (إعداد · رمز خاطئ/صحيح · حجب الدخول · جلسة صحيحة · تعطيل).

## 2026-09-16 — v4.9.0 (ملصقات المواقع القابلة للطباعة)

- صفحة **ملصقات المواقع** (`/labels`): اختيار مواقع متعددة ثم طباعة ورقة ملصقات تحتوي
  باركود **Code 39** لرمز الموقع + الاسم + المنطقة + المستودع، مع تجنّب قطع الملصق بين الصفحات.
- بند في القائمة الجانبية ضمن مجموعة العمليات التشغيلية.
- **QR/DataMatrix ومسح الكاميرا**: متبقٍّ — يحتاج مكتبة QR موثوقة وإضافة كاميرا (غير متوفرة في بيئة البناء الأوفلاين).

## 2026-09-16 — v4.7.0 (المواقع الهيكلية Bins)

- **كتالوج مواقع التخزين**: جدول `bins` (رمز · اسم · منطقة · مستودع · ملاحظات) مع مسارات
  (قائمة · استخدام · إنشاء · تحديث · أرشفة)، ومنع الأرشفة لموقع مستخدم في أصناف.
- تقرير استخدام المواقع (`/api/bins/usage`) يميّز الرموز المعروفة عن غير المعرّفة.
- حقل الموقع في الأصناف (`bin_code`) يدعم الآن إنشاء/تحديث على الخادم **وعلى الهاتف**،
  مع حقول إعادة الطلب/الأقصى/الأمان على الهاتف أيضًا.
- دعم كامل في الطبقة المحلية (IndexedDB): **69/69** في اختبار التطابق.

## 2026-09-16 — v4.6.0 (إشعارات خارجية Webhook)

- **إشعارات خارجية**: حقل `alert_webhook_url` في الإعدادات؛ وعند إنشاء/تصعيد أي تنبيه يرسل عامل
  التنبيهات حمولة JSON إلى العنوان المُعدّ (مهلة 5 ثوانٍ، محاولة واحدة بلا تعطيل للعامل).
- التحقق من العنوان (http/https · طول أقصى 500) والكتابة للمدير فقط.
- اختبار آلي بخادم استقبال محلي: **38/38**.

## 2026-09-16 — v4.5.0 (سندات الاستلام GRN)

- **سند استلام بفحص (GRN)**: مسودة لا تمسّ المخزون، كميات مطلوبة/مستلمة/مرفوضة لكل بند،
  سبب إلزامي للمرفوض، وترحيل يُدخل الكميات المقبولة فقط عبر دفتر الدفعات (خادم وهاتف).
- تقرير أداء المورّدين (`/api/receipts/summary`) مع نسبة الرفض.
- صفحة `/receipts` + بند في القائمة الجانبية.

## 2026-09-16 — v4.4.0 (تنفيذ توصيات التدقيق المهني)

### ميزات جديدة
- **الجرد الدوري (Cycle Counting)**: جلسات جرد أعمى، إدخال كميات لكل سطر، سبب إلزامي لكل فرق،
  واعتماد من المدير يرحّل الفروق كتسويات موثّقة — على الخادم وعلى الهاتف.
- **القيد العكسي**: مستند تعويضي مرتبط بالأصل بدل تعديل الحركة، مع منع العكس المزدوج وسبب موثّق.
- **فروق التحويلات**: تسجيل الكمية المستلمة فعليًا لكل سطر وفرقها وسببها + تقرير مخالفــات.
- **إعادة الطلب**: حقول الحد الأدنى لإعادة الطلب/الحد الأقصى/مخزون الأمان + تقرير كميات مقترحة للطلب.
- **مؤشرات الأداء**: تقرير KPI (دوران · تغطية · راكد · نفاد · دقة الجرد) وتصنيف ABC (80/15/5).
- **المواقع (رمز الموقع)**: حقل موقع تخزين للصنف يظهر في الجرد والتقارير.

### إصلاحات
- تسوية المخزون على الخادم كانت تُحدّث الرصيد دون دفتر الدفعات → انحراف دائم في الترصيد (أُصلح).
- أولوية المسار في الطبقة المحلية كانت تحجب مسار القيد العكسي (أُصلح).
- موضع أعمدة `transfer_lines` في مخطط سطح المكتب (أُصلح).

### اختبارات
- `warehouse-advanced-tests.mjs` 21/21 · تطابق الأوفلاين **56/56** · المراحل 1–8 126/126 · 52/52 وحدة.

## 2026-09-15 — v4.3.0 (ميزة استيراد الكتالوج)

### الميزة
- قالب Excel للكتالوج (ورقتا المواد والتجهيزات + القيم المرجعية + التعليمات) — بلا أي أعمدة كمية.
- مساران جديدان (للمدير فقط): `/api/catalog/import/preview` (تحليل بلا كتابة) و`/api/catalog/import` (تنفيذ ذرّي).
- وضعان: «إضافة فقط» و«إضافة وتحديث»، مع منع التنفيذ الجزئي عند وجود أخطاء.
- مصدر الوحدات والتصنيفات هو الكتالوج الفعلي (لا قائمة ثابتة).
- تبويب «استيراد الكتالوج» في شاشة الكتالوج: تنزيل القالب، رفع الملف، معاينة، وتقرير أخطاء CSV.

### الجودة
- عقد مشترك `lib/api-zod/src/catalog-import.ts` + 6 اختبارات وحدة.
- اختبار تكامل `docs/tests/catalog-import-tests.mjs` — 20 فحصًا (20/20).
- ملفات عيّنة: `fixtures/catalog-import/sample-valid.xlsx` و`sample-with-errors.xlsx`.

## 2026-09-14 — v4.3.0 (المرحلة 8: التقارير الموحّدة والطباعة)

### التقارير
- GET /api/reports/consolidated (للمدير): إجماليات + صف لكل مستودع + تنبيه الحد الأدنى لكل موقع.
- قسم «التقارير الموحّدة» في مركز المخزون.

### التحويلات (واجهة + طباعة)
- صفحة /transfers: سجل + إنشاء طلب + إرسال/استلام/رفض/إلغاء + طباعة.
- مستند تحويل للطباعة /transfers/:id/print (A4 مع خانات التوقيع).

### الاختبار والتوثيق
- اختبار تكامل جديد: docs/tests/phase8-consolidated-tests.mjs (10 فحوص — 10/10 ناجحة).
- توثيق المرحلة: docs/phase8-consolidated-reports-ar.md.

## 2026-09-14 — v4.3.0 (المرحلة 7: المزامنة متعددة المواقع)

### لوحة إدارة المزامنة
- مسار جديد GET /api/sync/overview (للمدير): هوية الجهاز، مستودع الموقع، صندوق الصادر، التعارضات، العُقد الموثوقة.
- لوحة في صفحة المزامنة تعرض القياسات وتُبرز ما يحتاج انتباهًا.

### دليل التشغيل
- دليل عربي لتشغيل عدة مواقع: التأسيس، الدورة الدورية، قواعد السلامة، ملاحظات 50 موقعًا.

### الاختبار والتوثيق
- اختبار تكامل جديد: docs/tests/phase7-sync-admin-tests.mjs (10 فحوص — 10/10 ناجحة).
- توثيق المرحلة: docs/phase7-multisite-sync-ar.md.

## 2026-09-14 — v4.2.0 (المرحلة 6: دورة التحويل)

### دورة التحويل
- جدولا transfers و transfer_lines + كيان مزامنة transfer.
- مسارات /api/transfers: إنشاء طلب، إرسال، استلام، رفض، إلغاء، مع كود ببادئة المستودع.
- الإرسال يخصم المصدر فورًا، والاستلام يزيد الوجهة فورًا (حركات عبر محرّك الحركات: FEFO/دفعات/تدقيق/مزامنة).
- إيصال استلام مبدئي (provisional) قبل وصول مستند الإرسال.
- سجل نظامي تلقائي (جهة/سبب) لحركة الإخراج.

### الرصيد لكل موقع
- GET /api/reports/stock-by-warehouse: رصيد لكل (صنف × مستودع) من سجل الدفعات + إجمالي لكل مستودع.

### الاختبار والتوثيق
- اختبار تكامل جديد: docs/tests/phase6-transfer-tests.mjs (19 فحصًا — 19/19 ناجح).
- توثيق المرحلة: docs/phase6-transfers-ar.md.

## 2026-09-14 — v4.2.0 (المرحلة 5: نموذج المستودعات)

### نموذج البيانات
- جدول warehouses (رمز فريد/اسم/نوع/حالة) وجدول document_sequences (عدّاد ترقيم لكل مستودع ونوع وسنة).
- warehouse_id على: system_settings و transactions و inventory_batches و equipment، مع إصلاحات تراكمية لقواعد البيانات القائمة.
- كيان مزامنة جديد warehouse + إضافة warehouseId إلى المواد/الدفعات/التجهيزات.

### السلوك
- المستودع المركزي الافتراضي (C) يُنشأ تلقائيًا عند الإقلاع، وتُنسب الصفوف القديمة إليه.
- مسارات /api/warehouses (قراءة للجميع، كتابة/تعيين للمدير) + رقم المستند التالي.
- ترقيم جديد للمستندات: CODE-TYPE-YEAR-NNNNNN عبر عدّاد ذرّي لكل مستودع.
- تبويب «المستودعات» في شاشة الكتالوج مع تعيين مستودع الجهاز.

### الاختبار والتوثيق
- اختبار تكامل جديد: docs/tests/phase5-warehouse-tests.mjs (16 فحصًا — 16/16 ناجح).
- توثيق المرحلة: docs/phase5-warehouse-model-ar.md.

## 2026-09-14 — v4.1.0 (المرحلة 4: مركز المخزون والترصيد)

### الترصيد
- مسار جديد GET /api/reports/reconciliation (للمدير): يقارن الرصيد المسجّل بمجموع الدفعات ويعرض الفروقات.
- شاشة «مركز المخزون» /inventory: مؤشرات + لوحة ترصيد + مواد تحت الحد الأدنى + التجهيزات حسب الحالة.

### التدقيق الحقلي
- تعديل تعريف مادة/تجهيز يسجّل تغييرات الحقول (before/after) في سجل التدقيق.

### الاختبار والتوثيق
- اختبار تكامل جديد: docs/tests/phase4-reconciliation-tests.mjs (12 فحصًا — 12/12 ناجح).
- توثيق المرحلة: docs/phase4-inventory-hub-ar.md.

## 2026-09-14 — v4.1.0 (المرحلة 3: حاكمية الاستيراد)

### سجل دفعات الاستيراد
- جدول جديد import_batches يسجّل كل استيراد: النوع، الوضع، المنفّذ، الأعداد، الحالة، وبيانات التراجع.
- مسارات جديدة: GET /api/import-batches و POST /api/import-batches/:id/rollback (للمدير فقط).
- الاستيراد (مواد/تجهيزات) يعيد batchId ويحفظ القيم السابقة للصفوف المحدَّثة.

### التراجع
- التراجع يؤرشف الصفوف المُنشأة ويُعيد القيم السابقة للصفوف المُحدَّثة، داخل معاملة واحدة، مع مزامنة كل صف.
- منع التراجع المزدوج (409)، ومنع التراجع الآلي عن دفعة أرصدة افتتاحية (409) لتجنّب إفساد الحركات.

### الاختبار والتوثيق
- اختبار تكامل جديد: docs/tests/phase3-import-governance-tests.mjs (19 فحصًا — 19/19 ناجح).
- توثيق المرحلة: docs/phase3-import-governance-ar.md.

## 2026-09-14 — v4.1.0 (المرحلة 2: الكتالوج والوحدات المعيارية)

### الكتالوج
- جدول جديد units للوحدات المعيارية (اسم فريد، رمز، حالة، ترتيب) + سجل مزامنة (كيان unit بأرشفة).
- مسارات جديدة /api/units: قراءة للجميع · كتابة وأرشفة وتوحيد للمدير فقط.
- شاشة «الكتالوج» الجديدة (/catalog): تبويبات الوحدات والمواد والتجهيزات + رابط في القائمة الجانبية.

### توحيد الوحدات
- تقرير استخدام يميّز الوحدات المعيارية عن النصّية المستخدمة في الأصناف.
- أداة توحيد تكتب الوحدة المعيارية على كل الأصناف وتُسجّل مزامنةً لكل صنف.
- منع أرشفة وحدة مستخدمة في أصناف (409).
- نموذج المادة يقرأ الوحدات من الكتالوج بدل قائمة الإعدادات النصّية.

### الاختبار والتوثيق
- اختبار تكامل جديد: docs/tests/phase2-catalog-units-tests.mjs (19 فحصًا — 19/19 ناجح).
- توثيق المرحلة: docs/phase2-catalog-units-ar.md.

## 2026-09-14 — v4.1.0 (المرحلة 1: الحاكمية والصلاحيات)

### الحاكمية والصلاحيات
- تعريفات الكتالوج (المواد والتجهيزات) والبيانات المرجعية (المستلمون) أصبحت **للمدير فقط** (إنشاء/تعديل/أرشفة/استيراد).
- أمين المستودع يحتفظ بالقراءة وبكل العمليات اليومية (إدخال/إخراج/عهد/تلف/مرتجع/تسوية).

### حماية البيانات
- أرشفة التجهيزات بدل الحذف الفعلي (is_active = false) مع استثنائها من القوائم.
- رمز تجهيز فريد (code) مع رفض التكرار (409).
- منع حذف تصنيف مستخدم في أصناف (409).
- قفل الهوية: منع تغيير رمز/وحدة مادة لها حركات مسجّلة (409).

### الواجهة
- مسارات النماذج التعريفية للمدير فقط (items/new, items/:id/edit, equipment/new, equipment/:id/edit).
- إخفاء أزرار الإضافة والاستيراد عن أمين المستودع.

### الاختبار والتوثيق
- اختبار تكامل جديد: docs/tests/phase1-authorization-tests.mjs (21 فحصًا — 21/21 ناجح).
- توثيق المرحلة: docs/phase1-governance-implementation-ar.md.
- اختبارات الوحدة القائمة: 46/46 ناجحة (بلا انحدار).

## 2026-09-10 — v4.0.2 (تحديث الهوية البصرية)

### الشعار والإصدارات
- استبدال الشعار الرئيسي بشعار مديرية صحة دمشق المرفق في الواجهة والتقارير المطبوعة وسندات العمليات.
- توحيد favicon وأيقونة الويب وموارد Android وشاشات البدء.
- إضافة شعار نافذة Windows إلى قالب Electron.
- رفع Android إلى `versionCode 402` و`versionName 4.0.2`.
- إضافة ملفات بناء الإصدار إلى `release-artifacts/v4.0.2`.

## 2026-09-06 — v4.0.1 (إصدار تصحيحي)

### توحيد الحزم
- توحيد رقم الإصدار `4.0.1` في واجهة الويب، بوابة التفعيل، API، Electron وAndroid.
- رفع Android إلى `versionCode 401` مع حزمة الويب الصحيحة لكل من النسخة العادية والمحمية.
- إصلاح تعارض alias في توقيع Gradle ودعم بناء APK الموقّع v1/v2.
- إضافة سكربت Node لإعادة تجميع Windows على Linux مع تطبيع مسارات ZIP القديمة.
- إضافة مولّدات تفعيل وملفات بناء مستقلة ضمن `release-artifacts/v4.0.1`.
- مفاتيح التوقيع الخاصة تبقى خارج Git، بينما تُتتبع مفاتيح التحقق العامة والبصمات فقط.

## 2026-09-03 — v4.0.0 (الإصدار النهائي)

### الترخيص الرقمي
- **ترخيص مربوط بالجهاز عبر الخادم**: جدول `license_state` في قاعدة البيانات — المعرف ثابت عبر كل المتصفحات والنوافذ وإعادة التشغيل (بدلاً من التخزين في المتصفح المتغير)
- راوتر عام `/api/license/status` + `/api/license/activate` (تحقق Ed25519 في الخادم عبر node:crypto)
- البوابة بمسارين: مسار الخادم (ويندوز) + مسار محلي (أندرويد) مع ترميم ذاتي لجدول الترخيص عند الإقلاع
- إصلاح «لا يدعم هذا الجهاز التحقق الآمن»: مسار noble الاحتياطي لعدم توفر WebCrypto Ed25519 + فحص الإصدار يرجع `version-mismatch` بدل `unsupported` المضلل
- إصلاح المسار المزدوج `/license/license/*` (404 دائم) وإدراج مفتاح التحقق العام داخل الحزمة

### المنافذ والشبكة المحلية
- منفذان ثابتان: الواجهة **41790** (0.0.0.0) وAPI **41789** — معرف الجهاز لم يعد يتغير بين التشغيلات
- CORS/CSRF لنطاقات LAN الخاصة + قواعد جدار حماية جاهزة

### الاستعادة والنسخ الاحتياطي
- **إصلاح جذري لفشل الاستعادة**: عبارات `ALTER TABLE transactions` كانت قبل إنشاء الجدول في `desktop-schema.sql` → أعمدة المزامنة مفقودة في القواعد الجديدة. نُقلت بعد الإنشاء + إزالة 8 backticks فاسدة من كتلة license_state
- ترميم ذاتي عند الإقلاع يضيف الأعمدة والفهرس وقيود FK إن غابت (يشفي القواعد القديمة)

### الحزم والإصدار
- حزمتا ويندوز (Offline + Protected) بإلكترون مطابق لـ v3.0.6 + حزمتا أندرويد موقعتان v1+v2
- مولّدات تفعيل (`KeyGenerator-v4.0.0-P-{Windows,Android}`) بتراخيص `appVersion="*"` تحفظ الترخيص في ملف نصي
- إصدار **Packages v4.0.0** منشور على GitHub Releases

### التنظيف
- حذف ~5GB ملفات مؤقتة وبيانات تطوير ومخلفات Replit وسكربتات اختبار مؤقتة (phase1-14)

---

## 2026-08-23

### إصدار Windows Portable `3.0.5`

- بناء نسخة Windows Portable من آخر commit على الفرع `main`.
- تبسيط إعدادات التشغيل المحلي (API مدمج مع قاعدة PGlite بدل PostgreSQL خارجي).
