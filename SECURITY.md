# سياسة الأمان — Security Policy

## الإبلاغ عن ثغرة

إن اكتشفت ثغرة أمنية: **لا تنشرها علناً**. افتح [Issue](https://github.com/ibrahims78/Damascus-Health-Directorate/issues) بعنوان يبدأ بـ `[security]` وصف مختصر، أو تواصل مباشرة مع المطور: إبراهيم الصيداوي — موبايل: 00963933706403، وسيتم الرد والمعالجة ثم النشر في إصدار مصحح.

## المزايا الأمنية المدمجة (v4.0.2)

| الطبقة | التفاصيل |
|---|---|
| الترخيص الرقمي | Ed25519 (مفاتيح منفصلة لكل منصة)، ترخيص مربوط بالجهاز، المفاتيح الخاصة خارج المستودع |
| كلمات المرور | تشفير أحادي الاتجاه، لا افتراضية (تُنشأ عند أول تشغيل)، إجبار التغيير، حد لمحاولات الدخول |
| الجلسات | جلسات موقعة + حماية CSRF على كل الطلبات المتغيرة |
| CORS | نطاقات الشبكة المحلية فقط (localhost / 127.0.0.1 / 192.168.x) |
| التشفير | حزم النسخ الاحتياطي والمزامنة مشفرة بكلمة مرور (لا تُقرأ من طرف Relay) |
| التدقيق | سجل تدقيق للعمليات الإدارية والمخزنية والاستعادة والتعارضات |
| التحقق من الحزم | فحص ومعاينة قبل أي استعادة، ونقاط تراجع قابلة للرجوع |
| التحقق من التراخيص | WebCrypto Ed25519 سريع + مسار noble احتياطي للبيئات القديمة |

## إرشادات النشر الآمن

1. غيّر كلمة مرور المدير فور إنشائها ولا تشاركها في المزامنة
2. احتفظ بمفاتيح `release-secrets/` على جهاز واحد موثوق فقط (غير مرفوعة إلى المستودع)
3. استخدم كلمة مرور قوية (8+ أحرف) لحزم النسخ والمزامنة
4. قيّد وصول LAN للأجهزة الموثوقة وفعّل قواعد جدار الحماية للمنفذين 41789/41790 فقط
5. تحقق من بصمات SHA256SUMS قبل استيراد أي حزمة على جهاز إنتاجي
