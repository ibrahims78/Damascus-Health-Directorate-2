{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "final:acceptance": "pnpm -w test && pnpm -w run typecheck"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:"
  },
  "dependencies": {
    "@workspace/backup-format": "workspace:*",
    "drizzle-orm": "catalog:",
    "postgres": "^3.4.9",
    "xlsx": "^0.18.5"
  }
}
